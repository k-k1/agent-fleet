bin/
*.log
**/*.md
# …but workspace-notes.md is a build input (baked in as the agents' usage policy).
!workspace-notes.md
# …and the assistant knowledge md files are //go:embed inputs of the agent binary
# (agent/assistants.go), so they must stay in the build context too.
!agent/knowledge/*.md
