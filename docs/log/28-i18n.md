# 28. Console の全面 i18n（UI 文言の多言語化 — 日本語↔英語） — 設計

**Status: ✅ Console 側は全フェーズ完了（`temp/sm5hcv2`）。P0 基盤＋Intl 集約 main マージ済（`ec51997`）／P1 設定タブ群
＋P2 中核画面（複数形 `tCount`・`<Trans>` インフラ含む全画面・モーダル・行）＋P3 バックエンド（未コード化
エラーの安定コード化＋ビルトインアシスタントのカタログ化＋ログイン/OAuth ページの Accept-Language 出し分け）
＋P4 ゲート＆整理（日本語専用機能のロケールゲート・`localeCompare`/`toLocaleString` の明示ロケール化・
ContextBar の t() 化）＋P5 回帰防止（AST ベースの裸和文リテラル検出 lint＋pending ratchet を `ci.yml` へ・
lint が洗い出した二次画面 62 file/約 517 箇所を全面移行しカタログ計 1738 キー・pending 空＝コンソール全体で
裸和文ゼロを強制）を実装済（front typecheck/test 268/build/i18n:lint 緑・Go build/vet/test workspace+CP 緑）。
残＝実フリート再ビルド後の言語切替の実機目視のみ。✅ P6 エージェント出力言語も完了
（返信サジェスト・圧縮引き継ぎ・作業計画・汎用/verb ペルソナ・ビルトイン persona 3 種を
ロケール分岐し、セッション報告は表示テキストと指示テキストを分離。`languageRule` の
非対称も解消 — 実績と残りは §6.6）** — Console UI を全面的に多言語対応する。
第 1 弾は日本語（ソース）↔英語。方式は**自前の軽量 i18n 層**（外部ライブラリを入れず、既存の
`lib/settings.ts` 外部ストアと `Intl.*` の上に構築）。範囲はフロント UI 文言＋バックエンドの
ユーザー向けエラー＋サーバ描画（ログイン/OAuth）ページ。エージェント出力言語（`outputLanguage`）は
当初「別軸として対象外」としていたが、公開準備で**既定の言語だけ**表示言語に追随させ（§4 の追記）、
**残りを P6 として本書に取り込んだ**。決定の経緯は [decisions/0016](../decisions/0016-i18n.md)。

---

## 0. 要旨

- **確定した方式**: 外部 i18n ライブラリを採用せず、`lib/settings.ts` の `useSyncExternalStore`
  ストアに `locale` を足し、その上に `t(key, vars)` ランタイム＋`Intl.*` フォーマッタを自作する。
  理由は (1) 既存が自前ストア/自前 Provider を貫く最小依存方針、(2) i18n は **React と非 React
  両方**（`client.ts` の `errText`、`notifications/store.ts` の `wording`、`tts.ts` の読み上げ）から
  呼ぶ必要があり、これは `settings.ts` が既に解いている二重消費と同型、(3) 複数形が要る箇所は約 25 で
  `Intl.PluralRules` で足り、日付/数値/相対時刻は元々重複フォーマッタを `Intl` へ集約したい整理と相乗り。
- **ロケールの単一真実源**: `settings.locale`。`theme` を丸ごと踏襲する（localStorage＋サーバ同期、
  初回描画前に適用）。ただし `theme` と違い**端末ローカルにしない**（言語は人単位で全端末追従）。
- **型でキー欠落を守る**: カタログは型付き（`keyof typeof ja`）。CI の `typecheck` がキー欠落・
  未翻訳キーを検出するので、ライブラリのツールに頼らずに回帰を防げる。
- **翻訳せず「ロケールゲート」する日本語専用機能**: TTS のかな読み変換・なろう形式ルビ・縦書きは
  `locale==='ja'`（実質 `provider==='voicevox'`）でゲート。IME 変換ガードは全ロケール維持。
- **バックエンド**: 既存の `{code, message}`＋`ERR_TEXT` 方式（docs/23 P0-3）にロケール層を足すだけ。
  `workspace/agent/` の未コード化和文エラー約 47 個をコード化し、ログイン/OAuth の Go 描画ページは
  React に乗らないので別途 `Accept-Language`／ロケール cookie で多言語化する。

## 1. 背景・現状

### 1.1 現状はゼロ、ただし seam は既にある

`package.json` に i18n ライブラリ無し。`Intl.*`／`navigator.language` の使用箇所ゼロ。
`console/index.html:2` は `<html lang="ja">` 固定で実行時に更新されない。UI 文言は全て和文ハードコード。

一方、過去の設計で i18n を見越した「継ぎ目」が既に入っている。これを土台にする:

- `core/api/client.ts:91-95` — `ERR_TEXT`（エラーコード→和文マップ）の直上に
  `// Add a locale layer over this map when i18n lands` と明記済み。
- `control-plane/errcodes.go` / `workspace/agent/errcodes.go` — 言語非依存の安定コードを定義。
  バックエンドは英語 developer fallback を返し、表示文言は Console が `code` から解決する（docs/23 P0-3）。
- `lib/settings.ts` — 自前 `useSyncExternalStore`。localStorage＋`/api/env/ui-prefs` サーバ同期、
  `getSettings()`（非 React）と `useSettings()`（React）の二重口。`applyTheme()` が
  `<html data-theme>` を初回描画前に適用。**`theme` が `locale` の完全なテンプレート**。

### 1.2 規模（実測 2026-07-16）

| 対象 | 数量 | 備考 |
|---|---|---|
| フロント翻訳対象の distinct 文字列 | 約 956 | tests と TTS/ルビ内部を除外後。123 ファイル（.tsx 81 / .ts 42） |
| うち JSX テキスト | 約 367 | `<button>保存</button>` 型 |
| うち属性（title/placeholder/aria/alt） | 約 273 | title が最多 |
| うち toast/通知/エラー | 約 104 | 中央集約 sink が高レバレッジ |
| うち補間テンプレート（難所） | 約 149 | プレースホルダ・複数形が要る |
| 複数形が要る箇所 | 約 25 | 英語は one/other、日本語は単一 |
| バックエンドのユーザー向け文字列 | 約 70〜90 | 大半は `workspace/agent/` |

Go 側 CJK 約 1,082 行の大半はコメント・ログ・エージェント向けプロンプト・enkana 発音辞書で、
ユーザーには出ない。サーバ描画 HTML は `html/template` 不使用、ログインページと OAuth
コールバックページのみが例外（`oauth_google.go` / `oauth_bitbucket.go`、計約 17 文字列）。

### 1.3 高レバレッジな中央集約 sink（最初に効く）

- `core/api/client.ts` の `ERR_TEXT`（＋`errText`）… サーバエラー表示の単一経路。
- `features/notifications/store.ts` の `wording()`（`:39`）… 通知種別ごとに
  `{title, body, speech}` を返す。**speech は読み上げ用の別形**なので `.speech` キー variant が要る。
- 設定のラベル定数 `lib/settings.ts`（`THEMES`/`ICON_SETS`/`SURFACE_COLORS`/`TTS_*`…）、
  `agents/registry.ts`、`lib/sessionview.ts`、`lib/agentModels.ts`。

## 2. 設計

### 2.1 ロケールの単一真実源（`settings.locale`）

`Settings` に `locale: string` を追加し、`theme` と同じ経路に載せる:

- 既定値: `navigator.language` から解決（`ja*`→`ja`、その他→`en`）。未対応言語は `ja` フォールバック。
- 端末ローカルにしない（`DEVICE_LOCAL` に入れない）＝サーバ同期し全端末で追従。
- `applyLocale(s)` を新設し、`applyTheme` と同じ 3 箇所（モジュールロード `settings.ts:622` 近傍、
  `setSettings` `:729`、`hydrateUIPrefs` `:713`）で呼ぶ。`applyLocale` は
  `document.documentElement.lang = s.locale` を書き、i18n ランタイムへ現ロケールを通知する。
- `LOCALES`（`THEMES` と同型の `{id,label}[]`。ラベルは各言語の自称: `日本語` / `English`）。

### 2.2 i18n ランタイム（`lib/i18n/`）

- `t(key, vars?)` — **React にも非 React にも同一関数**。現ロケールは `getSettings().locale` から引く
  （＝ストアが単一真実源。ライブラリの別ストアと二重化しない）。
- `useT()` — `useSyncExternalStore(subscribe, ...)` でロケール変更時に再レンダー（`useSettings` と同型）。
- 補間 — `{name}` プレースホルダを `vars` で置換。HTML はエスケープ（テキストノード前提）。
- 複数形 — `Intl.PluralRules(locale)` で `one/other/...` を選び、キーの複数形バリアント
  （`key.one`/`key.other`）を出す。日本語は常に `other` に解決。
- 型安全 — `ja` を正本カタログとし `export type MsgKey = keyof typeof jaFlat`。`t` は `MsgKey` 限定。
  `en` は `Record<MsgKey, string>` を満たすことを型で強制 → **未翻訳キーは tsc で落ちる**。
- `<Trans i18nKey ...>` — JSX 内マークアップ混在（例 `useSessionActions.tsx:121` の `<strong>` 埋め込み）用。
  プレースホルダをスロット（`<0>…</0>`）で受ける最小実装。
- カタログはロード時同梱（ja/en の 2 言語・各数十 KB で誤差）。3 言語目以降で肥大化したら
  非既定ロケールを `import()` 遅延ロードへ。

### 2.3 `Intl` フォーマッタの集約（`lib/intl.ts`）

i18n と独立に「やるべき整理」でもある。ロケール対応 util に一本化する:

- `relTime(locale, ...)` — 重複 3 個（`BranchList`/`CommitGraph`/`NotificationCenter`）を
  `Intl.RelativeTimeFormat` で統合。「本日まで/明日まで」等の特殊語はロケール別テーブル。
- `fmtDateTime(locale, ...)` — 手書き 4 個（`WsBar`/`MirrorView`/`ChatView`/`sessionview`）を
  `Intl.DateTimeFormat` へ。月日順はロケールで反転。
- `fmtNum(locale, ...)` — `Intl.NumberFormat`。`AdminTab.tsx:389` の素の `toLocaleString()`（＝ブラウザ
  ロケール追従の唯一の漏れ）も明示ロケール指定へ。単位語（トークン/件…）はカタログへ移す。
- `localeCompare` 使用箇所（`lib/project.ts` ほか）は明示ロケール＋`sensitivity` を渡す。

### 2.4 日本語専用機能は「翻訳」でなく「ゲート」

- TTS かな読み変換（`applyBuiltinReadings`/enkana/読み辞書/感情スタイル）、なろう形式ルビ
  （`readerText.ts`）、縦書き（`readerVertical`）は `locale==='ja'`（実質 `provider==='voicevox'`）で
  ゲート。英語 UI では Polly 等の英語音声へ流れ、かなパイプラインはスキップ。
- IME 変換ガード（`ChatView`/`MirrorView`/`TerminalView` の composition 判定）は
  **全ロケール維持**（中韓・アクセント入力も composition する）。
- CJK フォントスタックはそのまま（Latin でも無害）。ただし読みビューフォントのラベル
  `明朝`/`ゴシック` は UI 文言なので翻訳対象。短い和文ラベル前提の `white-space:nowrap` 幅
  （`viewer.css`/`ui.css`）は英語で溢れうるので P4 で再点検。

### 2.5 バックエンド（範囲内）

- **マップ済みコード**（`quota_sessions` ほか約 11）: バックエンド変更不要。`ERR_TEXT` にロケール層を足すだけ。
- **未コード化の和文エラー約 47 個**（`workspace/agent/` の chat_handlers/connections/session_title/
  assistants/session_paste/session_handlers/chat_providers）: 安定 `code` を割り当て、backend `message` を
  英語化、Console 側でコード→ロケール表示。両側同時変更ルール（errcodes.go のコメント）を踏襲。
- **ビルトインアシスタントの名前・説明・シード約 17 個**（`assistants.go`/`chat_handlers.go`）: 固定集合なので
  Console 側カタログへ寄せ、フロントで表示解決する（バックエンドはキー/id を返す）。
- **サーバ描画ページ約 17 個**（`oauth_google.go` の `loginPageHTML`、`oauth_bitbucket.go` の
  `bbCallbackPage`）: React に乗らないので Go 側で `Accept-Language`（ログイン前はロケール cookie 不在の
  可能性）で言語選択し、和文/英文の 2 版を持つ。
- **チャットのシステム notice 4 種**（`role=="notice"`: コンテキスト逼迫・超過・自動応答の停止・圧縮完了 —
  ADR [0033](../decisions/0033-stored-text-locale.md) で追加）: 会話 JSON に**本文でなくキーと引数**
  （`notice_key` / `notice_args`）を刻み、Console が `t()` で描く（`workspace/agent/chat_notice.go` ↔
  `console/src/features/chat/notice.ts`）。保存済みの会話も Lang 切替に追随する。`content` は正本言語（ja）の
  フォールバックとして残り、キー無しの旧レコードと未知キーだけがそこへ落ちる。Go 側にキーを足して
  カタログへの追加を忘れると英語 Console が黙って日本語へ落ちるので、`TestNoticeKeysExistInConsoleCatalogs`
  が ja/en 双方のカタログにキーの存在を検査する。
- **掃除候補の理由 9 種**（`GET /sessions/cleanup` の `reason`）: 同じ形。Agent が `reason_key`
  （`clean.reason.*`）を返し、Console が描く（`workspace/agent/session_cleanup.go` ↔
  `console/src/features/sessions/cleanupReason.ts`）。ここは**読み手が 2 人**——Console（訳す）と、
  同じ JSON を `list_cleanup_candidates` で受け取るアシスタント（カタログを持たない）——なので、
  `reason` に正本言語の文を載せたまま `reason_key` を足す形にした。旧 Agent と未知キーの
  フォールバックも兼ねる。キーの取りこぼしは `TestCleanupReasonKeysExistInConsoleCatalogs` が落とす。

## 3. 段階計画

- **P0 基盤 ✅（`ec51997`）**: `settings.locale`＋`applyLocale`＋`LOCALES`／i18n ランタイム（`t`/`tMaybe`/
  `useT`/`useLocale`／型付きキーで未翻訳を tsc 検出）／`<html lang>` 動的化／DisplayTab＋TopBar 外観
  ポップオーバーに言語ピッカー。**中央集約 sink 3 種を移行**（`ERR_TEXT` ロケール層・`wording()`・設定の
  テーマラベル定数）。ja/en カタログの初版。
- **Intl 集約 ✅（P4 の一部・`d91cb6b`＋`3606f87`）**: `lib/intl.ts`（`relTime`/`fmtDateTime`/`fmtNum`・
  `getLocale` 駆動・メモ化）で重複していた相対時刻 ×3＋日時 ×4＋数値 2 箇所を統合。管理モーダルの
  日付入力を `lang` でアプリロケール追従。
- **P1 設定タブ群 ✅（`temp/sm5hcv2`）**: Admin/Git/Agents/Ssm/Tts/Env/Display/Tokens/Ops ＋共有ラベル定数。
- **P2 中核画面 ✅（`temp/sm5hcv2`）**: 複数形（`tCount`＝`Intl.PluralRules`・`PluralKey` 型）と `<Trans>`
  （番号スロット `<0>…</0>`/`<1/>` の JSX 混在）のインフラを新設（`lib/i18n/index.ts`＋`lib/i18n/Trans.tsx`）。
  共有ラベル（registry `launchHintKey`／sessionview `stateInfo`・`exitLabel`）→ WsBar（複数形の主戦場・
  `agoText`/`untilText`/`expiryText`/使用状況チップ）→ TopBar 残り＋ChatView → MirrorView（最大・
  sub-component は非メモ化のため本体 `useT()` 1 つで部分木が追従・`t` は `tr` にエイリアス）→ sessions/
  repos/memo/scm のモーダル・行（LayoutMap/Memo/Send/useSessionActions〈confirm＋`<Trans>`〉/Archived/
  SessionRow/StartModal/LaunchModal/RepoRow/ProjectFiles）まで全移行。`tuiMemoryCost` は数値のみ＋
  `common.approx`。**除外**＝LLM プロンプト（要約 `MirrorView:386`）・回答判定の正規表現・engine/voice 名。
  各コミット typecheck/test 264/build 緑。残＝実機目視（言語切替）のみ。
- **P3 バックエンド ✅（`temp/sm5hcv2`）**: (a) `workspace/agent/` の未コード化和文エラー 24 個
  （計画時の「約 47 個」は対象文字列の概算。実装で実際にコード化した WriteErr は 24 個
  — 意味ごとの集約後の実数）を意味ごとに
  一意な安定コードへ（`errcodes.go` に定数追加。旧 `not_found`/`empty` 等の使い回しを解消）＋backend
  `message` を英語 developer fallback 化＋Console カタログに `err.<code>`（ja/en）追加。`chat_providers.go`
  の CLI 失敗 `fmt.Errorf`（detail 付き）も prefix を英語化（`generation_failed` と併せ、detail 保持のため
  意図的に非カタログ）。(b) ビルトインアシスタント 6 個の名前/説明を Console カタログ（`assistant.<id>.name`/
  `.desc`）へ寄せ、`features/chat/assistantI18n.ts` の `assistantName`/`assistantDesc`（builtin のみカタログ
  解決・Agent の和文は fallback）で AssistantSection/ChatView/StartModal/SendSelectionModal が表示解決。
  (c) `oauth_google.go`（login ページ＋`loginErrorBlock`）と `oauth_bitbucket.go`（`bbCallbackPage`）を
  `preferredUILang(r)`（Accept-Language を順走査・ja 既定）で ja/en 出し分け（React に乗らない Go 描画）。
- **P4 ゲート＆整理 ✅（`temp/sm5hcv2`）**: (1) 日本語専用機能を `locale==='ja'` でゲート＝TTS かな読み整形
  （`applyReadings` を `localizedReadings` で包む・`emotionOpts`・`enkana`/`particlePause` を ja 限定）／
  なろう形式ルビ（`buildReadUnits` に `ruby` 引数追加・非 ja は 《》｜ を素の文字扱い・`parseRuby` 本体は不変）／
  縦書き（`ReaderView` の `useLocale`＝非 ja で横書き固定＋トグル非表示）。IME 変換ガードは全ロケール維持。
  (2) 暗黙のブラウザロケール依存を明示化＝`intl.ts` に `compareText`（`localeCompare`＋`getLocale`＋
  `numeric`/`sensitivity:base`）を新設し `project.ts`/`ArchivedModal`/`AdminTab` の素の `localeCompare` を置換。
  `MirrorView`/`ContextBar` の素の `toLocaleString` を `fmtNum` へ。(3) P2 で漏れていた `ContextBar` の裸和文を
  `mirror.ctx_*` カタログへ（`useT` で言語追従）。(4) WsBar 特殊相対時刻は **P2 で既にカタログ化済**（`wsbar.ago_*`/
  `until_*`/`expiry_today`/`expiry_tomorrow`/`common.days_left`）＝追加不要と確認。(5) nowrap 幅は再点検の結果
  **修正不要**（フラグ済みは全て `max-width`＋`text-overflow:ellipsis` の値表示で英語は省略・トグルは auto 幅／
  実バグの KeysTab スマホ英語溢れは main `d82e7ae` で既修正）。(6) Playwright（Vite ハーネス）で ContextBar を
  実カタログ ja/en 切替レンダリング検証（英語溢れなし）。相対時刻/日時/数値の `Intl` 集約は上で完了。
- **P5 回帰防止 ✅（`temp/sm5hcv2`）**: (1) 裸和文リテラル検出チェック `console/scripts/i18n-lint.mjs`
  を新設。**TypeScript の AST** で JSX テキスト・文字列/テンプレートリテラル中の生 CJK を検出する
  （コメントは AST ノードでないので自動的に対象外＝表示に出る文言だけを拾う。正規表現より誤検出が少ない）。
  除外機構＝行末/直前行 `// i18n-exempt`・`i18n-exempt-start/end` 範囲・`i18n-exempt-file`・`ALLOW_FILES`
  （カタログ本体／TTS 読み辞書 `ttsText.ts`・`ttsDict.ts`・`tts.ts`／なろうルビ `readerText.ts`）。
  LLM プロンプト（MirrorView 要約・MemoTidyModal 整理）・VOICEVOX 話者/感情名・font 値・LOCALES 自称に
  除外マーカーを付与。`npm run i18n:lint`＋`ci.yml` の console ジョブに step 追加。
  (2) 翻訳網羅は既存どおり **tsc** が en/ja parity を型で担保（`en: Record<keyof typeof ja, string>`＝
  過不足を検出）＝lint 二重化は不要と判断。(3) **段階移行 ratchet**（`i18n-lint-pending.json`）＝移行待ち
  ファイルの違反は backlog として非致命、それ以外の新規違反のみ CI 落とし。ゼロ化したファイルは pending から
  外す。**この lint が洗い出した二次画面 62 file・約 517 箇所を第 1 波で全面移行**（viewer/scm/onboarding/
  notifications/共有 ui/assistant/mirror 実行設定/repos/project/sessions+memo/設定枠/AuthExpired）＝ja/en に
  437 キー追加・**pending は空**になり以後コンソール全体で裸和文ゼロを強制。移行は各クラスタを独立の
  namespace（view/scm/onb/noti/ui/asst/mgr/rp/pj/sx/set/auth＋common.mid_dot）で行い、`node
  scripts/i18n-lint.mjs --all` を成功オラクルにして各ファイルのゼロ化を確認。typecheck/test 268/build/
  i18n:lint（ゼロ）緑。残＝実フリート再ビルド後の言語切替の実機目視のみ。

- **P6 エージェント出力言語の完全対応 ✅（`temp/sjxmv6n`）**: Console の文言がゼロ和文になっても、
  **エージェントが生成して利用者が読むテキスト**は日本語の prompt に引きずられていた。判定表
  （§6.6）に沿って「利用者が読む生成物を作らせる prompt」を表示言語で分岐:
  (1) 返信サジェスト（セッション/チャット両方。★候補そのものの言語は会話の言語のまま — 候補は
  利用者がそのままセッションへ送る文で、言語を変えると相手セッションの出力言語まで反転する）／
  (2) 圧縮の引き継ぎ要約と作業計画（枠と 3 見出しを分岐。中身は「会話の主要言語で」を維持）／
  (3) 汎用ペルソナ・Files のアドホック verb（翻訳/要約）とシード文・既定タイトル／
  (4) ビルトイン persona 3 種（案内役・オペレーター・SRE）／
  (5) セッション報告＝**表示テキストと指示テキストの分離**（下記）。
  併せて `languageRule()` の非対称（auto は en のときだけ表示言語を強制）を解消し、auto を
  両ロケールとも本来の意味（入力言語に従う）へ戻した — 分岐の前提だった「persona が日本語」が
  消えたため。取りこぼしとして見つかった報告注入の区切り（【利用者からのメッセージ】）と
  `reportPreamble` も分岐。
  **報告の分離**: 報告カードは読み手が 2 人（カードを読む利用者と、同じ本文をプロンプトで受け取る
  オペレーター）で、1 本の日本語文字列に同居していたため §4 で対象外に置いていた。表示は事実だけを
  カタログキー＋引数で保存（notice と同じ ADR 0033 の流儀・`chat.report.*` を ja/en に 20 キー）、
  指示は保存せず `reportPromptFor` が注入の瞬間に表示言語で組む。自動走行/自動再開のトグルも
  注入時に読む（指示は「今どう動くか」なので、保留中に設定が変わったなら新しい設定が正しい）。
  旧レコード（`report_kind` 無し）は表示もプロンプトも保存済み Content に落ちる。
  検証は `prompt_lang_test.go`（英語側に日本語ゼロを prompt 関数の表で固定）＋
  `chat_report_lang_test.go`＋`assistants_test.go`（防御条項の日英ペア）＋console `report.test.ts`。

## 4. 対象外（明示）

- **モデルだけが読むプロンプト**（`mcp_stdio.go` の tool description 127 件、内部の判定指示、
  `memo.go` の整理プロンプトのような「生成物を利用者が読まない」もの）。
  **判断軸は「保存されているか」ではなく「誰が読むための文字列か」**（ADR
  [0033](../decisions/0033-stored-text-locale.md)）——保存レコードでも利用者が読む自前生成文は §2.5 のとおり
  範囲内で、P6 で分岐した。日本語でもモデルは解するので、訳す価値より日英 2 本を維持する
  コストが勝つ。
- **生成物の言語が UI ロケールで決まらないもの**: `fs_suggest_edit.go`（編集対象ファイルの言語に
  従う）、ブリッジの文面（`bridge_answer.go`/`bridge_approval.go` — 軸は**接続の通知言語**で、
  実装済み。UI ロケールと混ぜない）、返信サジェストの候補そのもの（会話の言語）。
- TTS 発音辞書（`enkana_dict.go`/`ttsText.ts` の読み変換内部）、コメント、テスト、
  `workspace/workspace-notes.md`。

> **追記（公開準備 2026-08-01）— 出力言語のうち「既定の言語」だけ先に追随させた。**
> 上の「範囲外（将来別途）」のうち、**利用者が読む答えの言語**は先に埋めた。理由は、
> `outputLanguage=auto`（既定）が実は中立ではないため — persona も `chatOutputRule` も
> 日本語で書かれているので、英語 Console の利用者にも日本語で返る。ADR 0033 の軸
> （誰が読む文字列か）で見れば表示言語に従うべき。
> - `chatOutputRule` をロケール分岐（全チャットのシステムプロンプトに載る唯一の共通文）。
> - `languageRule()` は auto のとき表示言語へフォールバック。**ただし `en` のときだけ**の
>   非対称: 日本語 Console では auto の従来の意味（入力言語に従う＝英語で書けば英語で返る）
>   を保つ。周りのプロンプトが既に日本語で補正が要らないため。persona 本文を英語化した
>   時点でこの非対称は見直す。
> - タイトル自動提案は既に表示言語に従っている（`titleLang()` = `uiLocale()`）。
>
> **追記の追記（P6 完了時）**: 上の非対称は**解消した**。persona（builtin 3 種・アドホック
> verb・汎用）と出力ルール・引き継ぎ前置きが両言語になり、「周りの prompt が日本語だから
> auto が中立でない」という前提そのものが消えたため。auto は両ロケールとも「入力言語に従う」
> に戻り、固定したい利用者は 設定 > 回答言語（ja/en）を選ぶ（Console の説明文は元から対称な
> 説明だったので、実装が説明に追いついた形）。副次的に、英語 Console で「これを日本語に訳して」
> が強制英語の指示と喧嘩しなくなる。persona 本文と `role=="report"` 本文も P6 で対応済み
> （§3 の P6）。引き続き範囲外は MCP tool description 127 件だけ。

## 5. リスク・未決

- カタログ規模（約 956＋バックエンド）を一気に移すのは大きい。P0 は基盤＋PoC に絞り、P1 以降を
  ファイル群単位で分割する。英訳は下訳を付けてレビューで磨く前提。
- サーバ描画ページのロケール源（ログイン前）が別経路。`Accept-Language` を第一候補、確定後は
  ロケール cookie を発行して以降の描画に使う。
- 複数形以外の言語差（語順・単位位置）はキー設計で吸収。文字列連結（`+` 組み立て。`MirrorView` の
  読み上げラベル等）はキー化時に 1 メッセージへ畳む。

## 6. 引き継ぎ（次セッションの作業手順）

文脈ゼロの別セッションが本書だけで進められるようにまとめる。

> **いまの現在地（2026-08-01）**: P0〜P6 まで完了。**Console 側の裸和文はゼロ**で CI の
> `i18n:lint` が新規混入を落とし、**エージェントが生成して利用者が読むテキストも表示言語に
> 追随する**（§6.6）。残っているのは実フリート再ビルド後の実機目視だけ。
> §6.1〜6.5 は Console の文言をキー化する作業（P1〜P5）の手順、§6.6 は P6（カタログではなく
> コード側のロケール分岐）の記録で、**新しい prompt を足すときはどちらに寄せるかを §6.6 の
> 判定表で決める**。

### 6.1 いま使えるインフラ（実装済み・参照実装）

- `console/src/lib/i18n/index.ts` — `t(key, vars?)`（型付き）/`tMaybe(key, vars?)`（動的キー・無ければ
  `undefined`）/`useT()`（React・ロケール変更で再レンダー）/`useLocale()`（現ロケール文字列）/
  `setLocale`/`getLocale`/`MsgKey`/`SUPPORTED_LOCALES`。**`t()` は非 React からも呼べる**。
- `console/src/lib/i18n/locales/ja.ts`（正本）・`en.ts`（`Record<keyof typeof ja, string>`＝**全キー必須**、
  過不足は `tsc` が落とす＝完全性ガード）。
- `console/src/lib/settings.ts` — `locale`（既定 `navigator.language`→ja fallback・`DEVICE_LOCAL` 外＝
  サーバ同期）・`LOCALES`・`applyLocale()`（`<html lang>`＋i18n へ push）。
- `console/src/lib/intl.ts` — `relTime`/`fmtDateTime`（＋`TIME_HM`/`DATETIME_FULL`）/`fmtNum`。
- 参照実装: `core/api/client.ts`（`errText`＋`tMaybe`）／`features/notifications/store.ts`（`wording()` の
  title/body/**speech**）／`features/settings/DisplayTab.tsx`＋`app/TopBar.tsx`（言語ピッカー＋`labelKey`）。

### 6.2 文字列 1 つを i18n 化するレシピ

1. **キーを決める**（`namespace.snake`。例 `git.repo_delete_confirm`）。既存の namespace（`err`/`notif`/
   `theme`/`settings`/`common`）に寄せる。
2. **`ja.ts` に `"key": "和文"` を追加、`en.ts` に同じキーで英訳を追加**（`en` に欠けると tsc が落ちる）。
3. **使用箇所を置換**:
   - React の JSX テキスト/属性: 先頭で `const tr = useT();` → `{tr("key")}` / `title={tr("key")}`。
   - 非 React（store/util）: `import { t } from ".../lib/i18n/index.ts";` → `t("key")`。
   - 補間: `tr("key", { name })`＋カタログ値に `{name}` プレースホルダ。
   - 実行時に決まる動的キー（例 backend の `err.<code>`）: `tMaybe("err." + code)`。
   - ラベル定数（`[{id,label}]`）: `label` を `labelKey: MsgKey` に変え、描画側で `tr(x.labelKey)`
     （`THEMES`/`REGION_THEMES` が済み例）。
4. `npm run typecheck` で未翻訳キーを検出。

### 6.3 まだ無い＝最初に作るインフラ（P2 冒頭で）

- **複数形**（英 one/other・日は単一。約 25 箇所）: `lib/intl.ts` に `pluralCategory(n)=new Intl.PluralRules
  (getLocale()).select(n)` を足し、`t` に「`vars.count` があれば `key_${category}` を引く」分岐（i18next 風
  suffix）を実装。カタログは `foo_one`/`foo_other` を両言語に。
- **`<Trans>`**（JSX 内マークアップ混在。例 `useSessionActions.tsx:121` の `<strong>` 埋め込み）: スロット
  （`<0>…</0>`）で子要素を差し込む最小コンポーネントを `lib/i18n` に新設。

### 6.4 翻訳しない／ゲートする（重要・触るな）

- **LLM プロンプト**は Console カタログ（`t()`）に入れない（表示でなくモデル挙動）: `MirrorView.tsx:382`
  （要約プロンプト）、`memo.go` など。**ただし「翻訳しない」と「ロケール分岐しない」は別物**——
  出力言語を左右する／生成物を利用者が読む prompt は、カタログではなく**コード側でロケール分岐**する
  （`getLocale()==='en' ? ... : ...`＋`i18n-exempt-start` マーカー。参照実装 =
  `useSessionActions.tsx` の handoff prompt、`session_title.go` の `titleSuggestPersona(lang)`、
  `chat.go` の `chatOutputRuleFor`/`languageRule`）。どちらに倒すかは P6（§6.6）の判定表に従う。
- **日本語専用機能は `locale==='ja'` でゲート**（P4）: TTS かな変換（`ttsText.ts`/`tts.ts` の
  `applyBuiltinReadings`/enkana/読み辞書/`emotionOf`）・なろう形式ルビ（`readerText.ts`）・縦書き
  （`readerVertical`）。これらの UI **ラベル**は翻訳するが、変換**ロジック**は触らない。
- **IME 変換ガード**（`ChatView`/`MirrorView`/`TerminalView` の composition 判定）は全ロケール維持。
- 対象外: VOICEVOX キャラ名・コメント・`*.test.ts`・`workspace/workspace-notes.md`。

### 6.5 フェーズ別 対象ファイル（着手順）

- **P1 設定タブ群**（自己完結・最大）: `src/features/settings/{AdminTab,GitTab,AgentsTab,SsmTab,TtsTab,
  EnvTab,DisplayTab,TokensTab,OpsTab}.tsx`。＋ラベル定数の残り（`lib/settings.ts` の `ICON_SETS`/
  `SURFACE_COLORS`/`SURFACE_TARGETS`/`TTS_*`/`OUTPUT_LANGUAGES`/`ASSISTANT_AGENTS`/`MIRROR_SEND_MODES`/
  `CLAUDE_MODELS`、`agents/registry.ts`、`lib/sessionview.ts`、`lib/agentModels.ts`、`controls.tsx` の
  「オン/オフ」）。密度上位: AdminTab(~150)/GitTab(55・`confirm()` 有)/AgentsTab(53)/SsmTab(49)/TtsTab(41)。
- **P2 中核画面**: `features/mirror/MirrorView.tsx`（＋`turnTts` の speech ラベル・`+` 連結を 1 キーへ）、
  `chat/ChatView.tsx`、`app/WsBar.tsx`（複数形の主戦場・`agoText`/`untilText`/`expiryText` の
  「本日まで/明日まで」はカタログキー化）、`app/TopBar.tsx` 残り、sessions/repos/memo/scm のモーダル・行
  （`StartModal`/`LaunchModal`/`RepoRow`/`useSessionActions`〈confirm＋JSX 混在〉/`ArchivedModal`/
  `SendSelectionModal`/`ProjectFiles` 等）。**§6.3 のインフラをここで先に用意**。
- **P3 バックエンド**（約 70〜90 文字列）: (a) `workspace/agent/` の未コード化和文エラー約 47 個
  （`chat_handlers`/`connections`/`session_title`/`assistants`/`session_paste`/`session_handlers`/
  `chat_providers.go`）にコードを割当て `errcodes.go` へ追加＋backend `message` を英語化＋Console の
  `err.<code>` キー追加（**両側同時**）。(b) ビルトインアシスタント名/説明/シード約 17 個
  （`assistants.go:196-225`, `chat_handlers.go:48-75`）を Console カタログへ。(c) サーバ描画ページ約 17 個
  （`control-plane/oauth_google.go` の `loginPageHTML`/`loginErrorBlock`、`oauth_bitbucket.go` の
  `bbCallbackPage`）を Go 側で `Accept-Language`／ロケール cookie で ja/en 出し分け（React に乗らない）。
- **P4 残り**: 上記ゲート適用、`localeCompare` 明示ロケール（`lib/project.ts`/`ArchivedModal`/`AdminTab`）、
  nowrap 幅の再点検（`viewer.css:418-425`/`ui.css:547-551`＝短い和文前提）、WsBar 特殊相対時刻、Playwright。
- **P5 回帰防止 ✅**: 裸和文リテラル禁止チェック `console/scripts/i18n-lint.mjs`（TS AST・§3 参照）＋
  `ci.yml` へ配線済。二次画面 62 file を全面移行し pending は空＝**新規の裸和文はどのファイルでも CI が落とす**。
  新しい和文を足すときは `t()/useT()` でカタログ化するか、翻訳対象外なら `// i18n-exempt[: 理由]` を付ける。
  棚卸しは `node scripts/i18n-lint.mjs --list`（backlog/BLOCK 別）、全違反の行位置は `--all`。

### 6.6 P6 エージェント出力言語（完了）

✅ 2026-08-01・`temp/sjxmv6n`。

**何が問題だったか。** Console の表示文言はゼロ和文になったが、**エージェントが生成して利用者が読む
テキスト**（アシスタントの回答・返信サジェスト・要約・作業計画・セッション報告）は、日本語で書かれた
prompt に引きずられて日本語で出ていた。英語 Console の利用者から見ると「UI は英語なのに中身が日本語」。
公開準備（2026-08-01）で**既定の言語だけ**先に追随させた（`chatOutputRuleFor`＋`languageRule` の
auto→表示言語フォールバック。§4 の追記）が、prompt 本体は日本語のまま＝言語シグナルが割れていた。
以下はその解消の記録で、**新しく prompt を足すときの判断基準**でもある。

**判定表（これが全て。ADR 0033「誰が読む文字列か」の適用）**

| prompt の性質 | 扱い |
|---|---|
| 生成結果を**利用者が読む**（回答・要約・件名・サジェスト・報告本文） | **ロケール分岐**（`titleSuggestPersona(lang)` と同型。両言語をそれぞれの言語で書く） |
| **モデルだけが読む**（ツールの説明・内部の判定指示） | **そのまま**。日本語でもモデルは解する。訳す価値より差分の維持コストが勝つ |
| 表示とプロンプトを**兼ねる**（`role=="report"` 本文） | **先に分離**が要る（§4）。分離せず訳すとオペレーターへの指示文まで変わる |

**やったこと（`workspace/agent/`・着手順＝依存の浅い順）**

| ファイル | 何をしたか |
|---|---|
| `session_suggest_reply.go` / `chat_suggest_reply.go` | `replySuggestPersona(lang)`＋指示文を分岐。セッション版とチャット版で重複していた指示文を `replySuggestInstructions(lang, counterpart)` に一本化 |
| `chat_compact.go` | `compactSummaryPromptFor` / `handoffPreambleFor` |
| `chat_plan.go` | `planShapeFor`（3 見出し）/ `planUpdateInstructionFor` / `planPreambleFor` / `planRefreshPersonaFor` / `planRefreshPrompt(c, lang)`、切り詰め注記、`blankPlan` に英語のプレースホルダ |
| `chat_handlers.go` / `chat.go` | `verbPersona(verb, lang)` / `seedFor(..., lang)` / 既定タイトル / `chatPersonaFor` |
| `assistants.go` | builtin persona 3 種を `personaFor(ja, en, lang)` で分岐（`builtinAssistants()` が `uiLocale()` を読む） |
| `chat.go` | `languageRule()` の非対称を解消（auto は両ロケールで「入力言語に従う」） |
| `chat_report.go` ＋新 `chat_report_text.go` | 表示テキストと指示テキストの分離（§3 の P6 参照）。`reportPreambleFor` と報告注入の区切りも分岐 |
| （対象外） `mcp_stdio.go` 257・`fs_suggest_edit.go` 15・`bridge_*.go` | §4 のとおり据え置き（モデルのみ／別の言語軸） |

**踏んだ地雷（次に prompt を足す人へ）**
- **生成物の言語軸は「表示言語」とは限らない。** 分岐したのは**指示文**で、生成物の言語は別問題:
  返信サジェストの候補は**会話の言語**（利用者がそのままセッションへ送る文なので、言語を変えると
  相手セッションの出力言語まで反転する）、要約と計画は**会話の主要言語**、翻訳 verb の言語ペアは
  **入力から決まる**。ここを一律に表示言語へ倒すと、直したはずの不具合を逆向きに作る。
- **operator persona を機械翻訳しない。** プロンプトインジェクション対策の指示を含み、誤訳＝防御の穴。
  日本語版と**段落の順序も対応関係も変えず**に書き、防御条項 6 件を日英ペアでテストに固定した
  （`TestOperatorPersonaInjectionGuardParity`）。片方だけ落ちるのが英訳で最も起きやすい壊れ方。
- **カタログの既存訳と綴りをそろえる。** 計画の英語見出しは Console の入力プレースホルダ
  （`chat.plan.placeholder`）と同じ Constraints / Given / Next up にした。食い違うと差分更新のたびに
  見出しが入れ替わる。案内役 persona が案内する先も表示名（"Fleet Operator"）で呼ぶ。
- **プレースホルダ判定・文字列一致は両言語ぶん更新する。** `blankPlan` は英語で指示した以上
  "none"/"no plan" が返ってくる（`isRateLimitResumePrompt` が `ja`/`en` 両方と比較しているのと同型）。
- **英語文字列に「・」「（）」を書かない。** 中点は U+30FB（カタカナブロック）なので、和文検出に
  引っかかる＝言語シグナルとしても割れている。実際に混入した。
- ブリッジ（Discord/Slack）の言語軸は**接続の通知言語**で、UI ロケールではない。混ぜない。

**成功オラクル（回帰防止）**
- `prompt_lang_test.go` — 分岐した prompt 関数を**表に集め**、英語側に日本語が 1 文字も無いこと／
  日本語側が英語版に置き換わっていないことを固定する。判定は仮名・漢字に加えて CJK 記号と全角も見る。
  **新しく prompt を分岐したらこの表に 1 行足すこと**（足し忘れると英語 Console にだけ日本語が残る）。
- `chat_report_lang_test.go` — 報告の分離（カードに指示が漏れない／注入は組む瞬間のロケールに従う／
  旧レコードは Content へ落ちる／カタログに ja/en 両方のキーがある）。
- console `report.test.ts`、`assistants_test.go`（防御条項の日英ペア）、`chat_lang_test.go`。
- 実機は英語 Console でアシスタントに 1 往復させ、回答・サジェスト・要約・報告カードが英語で出ることを目視（未実施）。

### 6.7 検証・運用ルール

- 各コミット前に `npm run typecheck`（en/ja 網羅を型で保証）／`npm test`（vitest・現 268 件）／
  `npm run i18n:lint`（裸和文ゼロ）／`npm run build`。4 つとも `ci.yml` がゲート。
- ブランチは**セッションのブランチにコミット**（自分から main へ切らない。マージはユーザー指示で）。
- コミットは**日本語**＋末尾に空行＋`Co-Authored-By: <モデル名> <noreply@提供元>`（`AGENTS.md`/`CONTRIBUTING.md`）。
- 実フリート再ビルド後の言語切替の実機目視が最終確認。
