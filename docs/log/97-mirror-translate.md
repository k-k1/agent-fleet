# 97. 回答をミラーで訳す（ターンを使わない翻訳）— 設計・実装

> 状態: **P0 完了**（2026-09-13）。利用者の要求は「エージェントの回答が英語だったとき、
> **エージェントのターンを使わずに**、ミラービュー上で指定言語に訳す」。
> 追加で確定した形: **全自動ではなく利用者がボタンを押す**／**翻訳元のハッシュでキャッシュし、
> セッションの削除まで保持**／**共有相手は押せない（所有者のみ）**。
> 関連: [28-i18n.md](28-i18n.md) §6.6（生成物の言語軸）/ [69](69-transcript-marks.md) §69.3（マークの
> アンカー）/ [46-usage-accounting.md](46-usage-accounting.md)（台帳の feature 軸）/
> [84](84-ai-assist-settings.md)（AI 補助の設定とモデル 2 ティア）。

## 97.1 何を作ったか

ミラーの各回答のフッタに **「訳す」/「原文」** を出す。押すとその回答の**文章パートだけ**が
表示言語（回答言語を固定していればその言語）に置き換わる。

- 生成は `chatx.OneShotHeadless`（`OneShotProse`）＝タイトル提案・返信候補・編集提案と同じ
  **バックエンド非依存のワンショット**。別プロセスで 1 回 CLI を回すだけなので、**セッション本体の
  エージェントには触れない**（ターンを消費しない・文脈が増えない・実行中の作業の後ろに並ばない）。
- 消費は台帳に `translate.mirror` として載る（`usagex.FeatureTranslate`）。
- 設定 > AI補助 の `mirrorTranslateEnabled` でオフにできる（既定オン。押すまで無料なので）。

## 97.2 決めたこと（と、その理由）

| 決定 | 理由 |
|---|---|
| **自動ではなくボタン** | 自動にすると「どの回答を」「いつ」訳すかを機械が決めることになり、**追記中のターンを訳して古い訳が残る**。押した時点＝そのターンが完結している、で解ける。ミラーは毎秒ポーリングするので、自動翻訳は「押していない人の分まで 1 回答 1 モデル実行」になる |
| **キャッシュ鍵＝原文のハッシュ** | `Idx`（行序数）は compaction でずれ、`anchorId` はフォークで別 ID が付く（本文は同一）。走行中のターンは育つので「ターン」は鍵にならない。**押した本文**だけが安定している |
| **鍵はサーバが自分で計算する** | クライアントが鍵を名乗れると、古いペインが無関係な訳を引き出せる。鍵はその回答の訳を決める唯一の根拠 |
| **言語も鍵の一部** | 回答言語を切り替えたときに前の言語の訳が出る |
| **パート単位** | コードフェンス・ツール出力を自然に飛ばせ、段落が 1 つ増えた回答は**増えた分だけ**再実行になる |
| **保存はセッション削除まで** | `removeSessionSideFiles`（`cleanup_ops.go`）に 1 行足す。**セッション名はスロット名で使い回される**ので、消し忘れると次にその枠に入った会話へ他人の訳が出る。ハッシュ鍵なので誤ヒットはまず起きないが、ディスクと回答のプライバシーは別問題。派生物なので**削除アーカイブには入れない**（マークと同じ） |
| **ポーリングに載せない** | `/messages` に訳を混ぜると (a) 毎 poll のバイトが増え（スマホの電池）、(b) CP の sessionWire に欄を足す 4 か所仕事が出る。別エンドポイント＋開いたとき 1 回の GET |
| **共有相手は押せない** | 押すと**所有者のワークスペースでモデルが走り、所有者のトークンを使う**。CP の `registerSessionShareRoutes` には対の経路を置かない＝共有ビューには capability が無く、ボタン自体が描かれない（`capabilities.ts` の原則） |

## 97.3 配線（5 か所）

| 層 | 何を足したか |
|---|---|
| Agent | `workspace/agent/session_translate.go`＝保存（`~/.config/agent-fleet/session-translations/<name>.json`）＋ `POST /sessions/{name}/translate` ＋ `GET /sessions/{name}/translations?lang=` |
| Agent | `errcodes.go` に `translate_disabled` / `translate_empty` / `translate_too_long`、`usagex` に `translate.mirror`、`uiprefs.MirrorTranslate()` |
| CP | `routes.go` の**所有者側 `rest`** に 2 行（共有側には置かない） |
| Console | `features/mirror/translate.ts`（純粋部分）＋ `useTranslate.ts`（配線）＋ `TranscriptTurn` のフッタと本文差し替え＋ `capabilities.ts` の `translate` |
| Console | 設定（`mirrorTranslateEnabled`・AI補助タブ）、カタログ ja/en、`turn.css` |

## 97.4 踏みどころ（次に触る人へ）

- **ハッシュは日英 2 実装で、UTF-8 バイト列を数える。** ブラウザ側の唯一の組み込みダイジェスト
  （SubtleCrypto）は非同期で、しかも jsdom に無い＝描画テストが書けなくなる。なので FNV-1a を
  Go と TS の両方に置いた。**JS が UTF-16 コード単位を数えると ASCII のテストは全部通り、
  実際の回答だけ鍵が食い違う**ので、ベクタに日本語と絵文字を入れて両側で固定している
  （`session_translate_test.go` / `translate.test.ts`）。
- **マークは訳文の上に置けない。** マークは引用文字列でアンカーする（§69.3）ので、訳文に
  当てると「たまたま一致した別の文」に線が引かれる。訳を表示しているパートは `markRoot` を
  外す＝マークは原文に残る。
- **訳文は同じ MarkdownView に流す。** 別の描画経路を作ると Markdown の不具合が 2 セットになる
  （全角パイプ表・`<…>` の消失などは既に踏んでいる）。
- **コピーは「いま見えている方」を返す。** 訳を見ている人に読めない原文を渡すのは驚きになる。
- **返信サジェストに訳文を食わせない。** サジェストの言語軸は「会話の言語」で、訳文を渡すと
  相手セッションの出力言語まで反転する（§6.6 の地雷そのもの）。今回は経路を分けてあるだけで、
  将来まとめようとしたときに壊れる箇所。
- **persona はロケール分岐しない。** 出力言語がパラメタそのものなので、指示文を 2 本持っても
  読者が見るものは変わらず、維持コストだけが増える（`fs_suggest_edit.go` と同じ判断・§6.6 の
  判定表の「生成物の言語が UI ロケールで決まらないもの」）。

## 97.5 検証

- Go: `session_translate_test.go`（ハッシュのベクタ／キャッシュ命中で**モデルが走らないこと**を
  実行回数で確認／言語が鍵の一部／パート単位の再利用／入力拒否 4 種で**1 回も消費しないこと**／
  設定オフ／一覧の言語分離／フェンスの巻き戻し／上限の追い出し／削除連動）。
  **陽性対照**: `lookupTranslation` を常に nil に潰すと「二度目もキャッシュに当たらない」で
  2 本が赤くなることを確認済み（潰しは Edit で戻した）。
- Console: `translate.test.ts`（Go と同じハッシュベクタ・判定 8 件）、
  `TranslateTurn.dom.test.tsx`（ボタンが出る/出ない 4 条件・押下で本文が入れ替わり原文に戻る・
  保持済みは通信しない・訳文から markRoot が外れる・ツール痕跡は無傷）。
- 全体: `workspace/agent` 2974 / `control-plane` 1475 / Console 2582 いずれも緑。
  typecheck・`i18n:lint` も緑（typecheck は故意の型エラーで赤くなることを確認済み）。
### 実機 1（2026-09-13・このホスト＝配備済みの workspace-agent）

Agent の HTTP に直接 1 回投げた（434 バイトの英文 Markdown・箇条書き＋インラインコード＋パス＋強調）。

| 見たこと | 結果 |
|---|---|
| 1 回目（生成あり） | **17 秒**・HTTP 200・訳文は Markdown の構造・`inline code`・パス・`**強調**` をすべて原文どおり保持 |
| 2 回目（同じ本文） | **0 秒**・`cached: true`・本文は 1 回目と同一＝モデル実行なし |
| 保存 | `~/.config/agent-fleet/session-translations/<name>.json`（649 バイト・0600） |
| 台帳 | `feature=translate.mirror` / `trigger=manual` / `ok=true` / `ms=16885` の行が 1 本 |

🔥 **そして台帳が応答の嘘を暴いた。** 応答は `model: "sonnet"` と言っていたが、台帳の行は
`kind=agy` / `measured=none`（トークン 0）だった。**このホストの 設定 > AI補助 の優先順位が
`['agy','claude',…]`** で、`OneShotHeadless` は先頭の使えるバックエンド＝agy（Gemini 3.5 Flash）で
走ったのが正しい挙動である。嘘だったのは**こちらが返していた `model`** で、実行結果ではなく
**要求値**（`translateModel()`＝claude 用の既定）をそのまま表示用に載せていた。

これは ADR 0029 §1 が `kind` について禁じている誤り——「要求ではなく実行結果を書く」——を、
台帳ではなく**画面の側で**やり直したものである。`OneShotHeadless` は実際に走った backend/model を
**返さない**（内側で台帳に書く）ので、この層に真実は無い。よって `model` は応答からもストアからも
**消した**。回帰試験は `TestTranslateReplyClaimsNoModel`（応答・ストアの双方に `model`/`kind` が
現れないこと。戻すと赤くなることを確認済み）。

> 副産物として、**訳文の品質は agy の Flash でも十分だった**（上の表の 1 回目がそれ）。
> claude/sonnet との比較は未実施。

- **未実施**: Console の画面からの操作（ボタンの見え方・訳の出方）。上の実機は Agent の API に
  直接投げたもので、描画は jsdom の試験までしか通っていない。
- **未実施**: 長い回答（数 KB〜32 KiB 近く）での所要時間。17 秒は 434 バイトの値である。

## 97.6 ついでに直した既存不具合（マークのアンカー）

訳文から `markRoot` を外す試験を書いたら、**訳す前から root が空**だった。原因は翻訳と無関係で、
作業痕跡を畳むターンでは `renderAssistantParts` にスライスが渡るのに `turn.origins` を
スライス基準の添字で引いていたこと。**ツールを 1 つでも使ったターンの最終回答に印を置けない**
（選択ピルが出ない）状態だった。オフセットを渡して修正し、[69](69-transcript-marks.md) §69.8 に
🔴 訂正を足した。**パート 1 つのターンでは再現しない**ので、転写の描画試験には必ず
「ツール＋本文」のターンを入れる。

## 97.7 残（P1 候補）

- Console の画面で押してみる（実機 1 は API 直叩き）。長い回答での所要時間も測る。
- 「何が訳したか」を出したいなら、`OneShotHeadless` が**実際に走った kind/model を返す**ように
  広げるのが唯一の正直な道（呼び出し 4 箇所に影響。今は台帳だけが知っている）。
- 共有ビューで**所有者が既に訳したもの**だけを読めるようにするか（今は原文のみ）。
- ターンをまたいだ「この会話をぜんぶ訳す」。P0 は 1 回答 1 押下に絞った。
- チャット（アシスタント）側は対象外のまま。あちらの回答は既に表示言語に追随する（§6.6 の P6）。
