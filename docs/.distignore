# docs/ の配布・公開から除外する internal denylist（docs/35 §35.4.3、2026-07-21 精査）。
# 1 行 1 パターン（docs/ 相対、tar --exclude 形式）。release 工程（deploy/compose/
# release.sh の docs ステージング → CP イメージの DOCS_SRC、P2 では native tar C の
# docs/ 同梱）で適用される。自社 dev / 自社運用ビルド（run-dev.sh）は全ツリーのまま。
#
# 除外理由: 運用ログ系に自ホストの稼働情報・個人情報が集中しているため（精査結果は
# docs/35 §35.4.3 の表を参照）。設計文書（番号付き・dev/・guide/・decisions/）はクリーン。
HANDOFF.md
CHANGELOG-handoff.md
talk
history
