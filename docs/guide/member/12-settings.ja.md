# 12. 設定 — ⚙設定モーダルの全タブ

[English](12-settings.md) | 日本語

> 対象: 「あの設定はどこにあるのか」「この項目は何を変えるのか」を確かめたいメンバー。
> 個々の設定の**使いどころ**は各章に書いてあるので、この章は**地図**として使ってください。
> 深い説明がある項目はその章へのリンクを置いています。

## 開き方と構成

画面最上部のバーの **⚙設定** で開きます。左レールが 3 つのグループに分かれています。

| グループ | 何が入るか |
|---|---|
| **個人設定** | 表示 / アカウント / キー操作 / 読み上げ / 通知 / アシスタント / エージェントへの指示 |
| **接続** | エージェント / Gitホスティング / 運用・監視 / チャット連携 / MCP サーバー / MCPトークン |
| **ワークスペース** | 使用量 / エージェントメモリ / ツールチェーン / AWS SSM / 内部リポジトリ / 危険な操作 |

- 最後に開いたタブを覚えているので、次に開くとそこから始まります。
- **スマホでは一覧 → 詳細のドリルダウン**です。戻る操作で一覧に戻り、もう一度で閉じます。
- チームの**メンバー・上限・監査**の管理は、権限のある人にだけ出る**別のモーダル**（盾のアイコン）です
  （[admin/](../admin/README.ja.md)）。
- 一部のタブは**ワークスペースの起動が必要**です（設定の実体がワークスペース内にあるため）。
  停止中はその旨と「ワークスペースを起動」ボタンが出ます。

### いつ反映されるか

これを取り違えると「設定したのに変わらない」になります。

| タイミング | 対象 |
|---|---|
| **即時** | 表示・キー操作・読み上げ・通知、接続の追加と削除 |
| **次に起動するセッションから** | エージェントの動作設定、エージェントへの指示、セッション間メッセージ、MCP サーバー |
| **次のチャット送信から** | アシスタントの設定、運用・監視の接続（アシスタントで使う場合） |
| **ワークスペースの停止 → 起動** | ツールチェーン（タイムゾーン・言語のバージョン） |

**保存先も 2 種類**あります。テーマ・背景色・メイン領域の配置は**この端末だけ**に保存され、
それ以外（フォント・文字サイズなど）はサーバーに保存されて別の PC やブラウザでも追従します。

---

## 個人設定

### 表示

見た目まわりをまとめて扱います。

- **カラーテーマ** — アプリ全体に加えて、**セッション** / **共有セッション** / **アシスタント**の面ごとに
  色を変えられます（「継承」でアプリのテーマに従います）。面ごとに色を変えておくと、ペインを並べたときに
  どれがどれか一目で分かります。
- **ターミナル** — フォントと文字サイズ（[03](03-terminal.ja.md)）。
- **ファイルビューア** — タブ幅・行番号・折り返し・ミニマップ、Markdown 表示（[05](05-files.ja.md)）。
- **朗読ビュー** / **ファイルアイコン**（アイコンセット）。
- **メイン領域の配置** — **分割ペイン**（境界をドラッグして並べる）と**タブ付きグリッド**
  （各マスをタブで切り替える）を選べます。**この端末だけ**に保存され、2 つの配置はそれぞれ別に
  覚えているので行き来しても崩れません（[03](03-terminal.ja.md)）。よく切り替えるなら、
  画面最上部のバーの**外観**（ペンキ缶）からも同じ切り替えができます。

### アカウント

いまのアカウントに紐づいた**サインイン方法**（Google / Microsoft / GitHub …）の一覧です。
どの方法で入っても、同じワークスペース・同じホーム・同じ設定になります。

- **サインイン方法を追加** — 会社が複数のサインイン方法を用意している場合（例: 本社は Microsoft、
  出向先の子会社は GitHub）、2 つ目を自分のアカウントに足せます。押すとその方式のログインへ飛び、
  戻ってくると一覧に増えます。
- 追加できるのは、**いまのアカウントと同じメールアドレスを名乗る方法**だけです。別のアドレスの
  アカウント同士を 1 つにまとめることはできません（後から戻せないため）。
- その方式の入場条件（GitHub なら**組織のメンバーであること**、許可されたメールドメイン）も
  満たしている必要があります。紐づけは条件の抜け道ではありません。
- すでに**別の人のアカウント**で使われている方式は追加できません。
- ログイン画面で「このメールアドレスは、すでに別のサインイン方法で使われています」と出た人は、
  **いつも使っている方法でログインしてから**ここで足すのが正しい直し方です。
- **解除** — 使わなくなった方法は一覧から外せます。ワークスペース・ホーム・秘密情報はそのままで、
  あとから同じ方法をもう一度追加できます。外せないのは次の 2 つで、ボタンが押せない理由は
  ボタンにマウスを乗せると読めます:
  - **いまサインインしている方法** — 先に別の方法でサインインし直してから解除してください。
  - **残り 1 つの方法** — 解除するとどの方法でも入れなくなり、自分では戻せません
    （パスワードもメール送信も無い仕組みのため）。

### キー操作

- **ショートカットの割り当て** — 直接キー（`Alt+1` など）とアプリ全体の 3 キーを変更できます。
  リーダー配下のシーケンス（例: リーダー → `p` → `r`）は構造上の操作なので変更できません。
  **?** でいつでも一覧を開けます。
- **端末入力の優先** — オンにすると、端末にフォーカスがある間は Ctrl 系のキーをすべて端末へ渡します。
  アプリ側はリーダーだけが生き、そこからコマンドメニュー／パレットに到達できます。
- **shell / SSM 端末へキーをすべて渡す** — 上よりさらに強く、リーダー（Ctrl/⌘+K）とパレット（Ctrl/⌘+P）も
  渡して純粋な端末にします。対象は shell / SSM だけで、エージェントの端末は従来どおりです。既定オフ。
- **送信キー** — **Ctrl+Enter 送信（Enter で改行・既定）** と **Enter 送信（Shift+Enter で改行）** から
  選びます。セッションのチャットとアシスタントのチャットに共通で効きます。
- **返信サジェスト** — コンポーザーの上に短い返信の候補チップを出します（[07](07-chat-memo.ja.md)）。
  ここで学習済みの候補の消去・ピン留めの解除ができます。
- **AI返信候補の✨ボタン** — 直近の会話から候補を生成するボタンの表示。既定オン。

### 読み上げ

セッションやアシスタントの回答を音声で読み上げます。

- **声** — 音声エンジン（ずんだもん（VOICEVOX）/ Polly / 自動）、話者、**セッションごとに声を変える**
  （キャラクターのプールから割り当て）、感情の読み分け、読み上げ速度。
- **自動読み上げ** — 新しい回答を自動で読む、**作業過程を小声で読む**、開いている全ペインで読む、
  長い回答は要約して読む、確認・質問を読み上げる。
- **読み方** — コード片を省略、助詞のあとで一呼吸、英語をカタカナ読み、**読み仮名辞書**
  （`表記=読み` を 1 行に 1 件）。
- **詳細** — バックグラウンドでの再生と音量、ペイン位置に合わせた左右振り、音声キャッシュ。
- **音声通知** — セッションの状態変化と、利用上限のリセットを声で知らせます。
- 「設定を初期状態にリセット」で読み上げ設定だけを戻せます（読み仮名辞書は残ります）。

### 通知

- **音声通知**のオン・オフ（読み上げタブの詳細設定への入口）。
- **サービス通知** — Discord / Slack へ**接続を切らずに送信だけ**を止められます。
  接続そのものは「チャット連携」タブです（[08](08-advanced.ja.md)）。
- **デスクトップ通知を許可** — ブラウザの通知許可を求めます。
- 通知の履歴は、画面最上部のベルから開く**通知センター**（過去 7 日間）で読めます。

### アシスタント

アシスタントチャットとフリート・オペレーターの挙動です（[07](07-chat-memo.ja.md)・
[11](11-fleet-operator.ja.md)）。

- **回答言語** — 入力に合わせる / 日本語 / English。
- **タイトルのAI提案** — チャットの名前変更ダイアログの「AIに提案してもらう」の有無。
- **エージェント優先順位** — 上から見て最初に接続済みの CLI をアシスタントに使います。
- **アシスタントのモデル** / **タイトル・サジェストのモデル** — CLI ごとに指定します。
  「推奨（現在: …）」は接続中のカタログから安全な既定を選び、いま何に解決されているかも表示します。
- **セッション報告への自動応答** — 報告が届いたときオペレーターが自動で 1 ターン動きます。
  **自動応答の上限回数**（既定 10・最大 50・無制限にはできません）、**自動応答のモデル**（報告の確認は
  定型作業なので軽量モデルにすると節約になります）、**束ね時間**（この時間内の報告をまとめて 1 回で処理）、
  **静かな完了報告**（正常完了では自動応答せずカードと通知だけにする）。
- **自動走行** — 質問とプラン承認まで自動で進めます。既定オフ（[11](11-fleet-operator.ja.md)）。
- **中断時の自動再開** — 接続断や一時的なレート制限で切れたターンを再開させます。既定オン。
- **コンテキストの自動圧縮**と**自動圧縮の閾値** — 長くなった会話を自動で要約引き継ぎします。
- **セッション出力の取得上限** — オペレーターがセッションの出力を読むときの上限（既定 32 KiB）。
  大きいほど会話に蓄積されてトークン消費が増えます。全文はいつでもチャット表示で読めます。
- **外観** — アシスタント面のテーマと背景色（この端末だけに保存）。

### エージェントへの指示

このワークスペースで新しく起動するエージェントに、自分専用の指示を足します。
配り先ごとに「どのファイルに書かれたか」「いま効いているか」が並びます。
詳しくは [06 エージェント](06-agents.ja.md#エージェントへの指示自分の働き方を一度だけ書く)。

---

## 接続

### エージェント

claude / codex / opencode / GitHub Copilot / Cursor / Kiro（と実験枠の Antigravity）の接続と動作設定です。
既定モデル、**使わないモデル**、**Claude の追加モデル**、思考の展開、RTK など。
**セッション**グループには、タイトル自動提案、**セッション間メッセージ**、利用上限リセット後の自動再開、
中断したターンの自動再開があります。→ [06 エージェント](06-agents.ja.md)・
[02 セッション](02-sessions.ja.md#セッション同士でメッセージを送る)

### Gitホスティング

GitHub / Bitbucket の連携です（clone / push の認証）。**GitHub の連携は GitHub Copilot の接続も兼ねます**。
→ [04 リポジトリと git](04-git.ja.md)

### 運用・監視

PagerDuty / Grafana / CloudWatch / AWS を接続し、**SRE アシスタント**が実データを見ながら
インシデントの壁打ちをできるようにします。CloudWatch と AWS は SSM 接続のプロファイルを選ぶだけで、
秘密の入力はありません。AWS の**書き込みツールは既定オフ**です。
→ [10 運用ツール連携](10-ops-mcp-poc.ja.md)

### チャット連携

Discord / Slack の Bot を接続し、セッションの進捗をチャットで受けて返信で操縦します。
→ [08 一歩進んだ使い方](08-advanced.ja.md#discord--slack-と連携するチャットブリッジ)

### MCP サーバー

**自分で使う MCP サーバーをここに登録**すると、アシスタントやセッションから使えるようになります。
社内 Wiki、チケット管理、ドキュメント検索など、Agent Fleet が組み込みで持っていない道具を足す場所です。

- **接続方式** — **stdio**（ワークスペース内で実行ファイルを起動。コマンド・引数・環境変数を指定）か、
  **リモート (HTTP)**（URL とヘッダを指定）。
  **環境変数とヘッダの値は暗号化して保存**され、サーバーの起動時にだけ渡されます
  （設定ファイルに平文で残りません）。資格情報は URL ではなくヘッダに入れてください。
- **利用先** — **アシスタント** / **セッション**のどちらに配るかを選びます（両方外すと、登録は残りますが
  どこにも配られません）。**対象エージェント**を選ばなければ全エージェントが対象です。
- **接続テスト** — サーバー名・バージョン・ツール数と応答時間を確認できます。
- **有効 / 無効** — 無効にすると、定義を残したままどこにも配られなくなります。
- セッションへの反映は**次に起動するセッションから**です。アシスタントは各アシスタントの編集画面で
  「MCP サーバー」から選びます（[07](07-chat-memo.ja.md)）。
- 一覧には出自のラベルが付きます。**個人**（自分の登録）、**テナント**（管理者が配布。同じ名前の個人登録は
  使われません。値の入力だけをメンバーに求める形の配布もあります）、**組み込み**（Agent Fleet 自身の
  サーバーと、運用・監視の連携）。
- 外向き通信が制限されたデプロイでは、接続先ホストの**許可を申請**して管理者の承認を待つ導線が出ます。
- リポジトリ側に置かれた MCP の定義（`.mcp.json` など）は、リポジトリ行のメニューから開く
  **プロジェクト設定**で、エージェント別の効き方と警告（秘密情報が Git 管理下にある、名前の衝突、
  ファイル間で内容が食い違う、など）を確認できます。

> **Agent Fleet 自身の MCP** は「組み込み」として最初から全セッションに配られています（設定不要）。
> セッションが完了を申告したり、次のセッションへの引き継ぎ案を出したりするのに使われています。

### MCPトークン

手元の PC の Claude Code / Claude Desktop から自分のワークスペースを遠隔操作するためのトークンです。
→ [08 一歩進んだ使い方](08-advanced.ja.md#外部の-claude-から自分のワークスペースを操作するmcp)

---

## ワークスペース

### 使用量

**何にトークンを使ったか**の台帳です。セッション本体だけでなく、Agent Fleet が裏で撃つ補助呼び出し
（タイトル提案・要約引き継ぎ・返信候補など）も同じ物差しで並びます。

- **期間** — 24時間 / 7日 / 30日。
- **割り方** — 機能 / エージェント / モデル / セッションの出自（人が開始・オペレーターが作成・
  定時実行が作成・引き継ぎ）/ 注入元（ユーザー・自動・定時実行・オペレーター・ブリッジ など）。
- **指標** — 消費トークン / 呼び出し回数 / キャッシュ読取 / **API換算相当額**（サブスクで使っていても、
  もし API で同じことをしたらいくら相当か、の目安）。
- 時系列グラフの系列をクリックすると、その系列だけに絞り込めます。**機能 × モデル**の表もあります。
- **計測できている範囲**が明示されます。トークンを報告しない呼び出しは「回数だけ数えている」と出ます
  （消費ゼロという意味ではありません）。
- **rtk 効果** — トークン節約の累計・平均節約率・実行コマンド数を日次 / 週次 / 月次で表示します
  （RTK は [06](06-agents.ja.md#rtkトークン節約)）。

### エージェントメモリ

エージェントが自分で貯めていく記憶（claude の自動メモリ、codex の memories）の**版管理**です。
「余計なことを覚えてしまった」「いつからおかしくなった」を後から直せます。

- **対象** — 版管理できるメモリの一覧と、ファイル数・サイズ・最終スナップショット。
  codex はメモリ機能が既定で無効なので、使うならここで有効にします。
- **自動スナップショット** — エージェントが停止して数分たつと自動で取得します（変更が無ければ積みません）。
  「今すぐスナップショット」で手動取得もできます。運用側の設定で自動取得が止められている環境もあります。
- **履歴** — 時刻と契機（自動 / 手動 / 巻き戻し前 / 巻き戻し / 取り込み）が新しい順に並びます。
  日時を指定してその時点へジャンプすることもできます。
- **この時点に戻す** — 範囲（すべて / 選んで戻す）を指定して巻き戻します。
  **戻す直前の状態も自動で記録される**ので、この操作自体を後から取り消せます。
  対象のエージェントが実行中のときは警告が出ます。
- **持ち出し / 取り込み** — bundle（全履歴）または tar.gz（最新のみ）。書き出す内容に秘密情報らしき
  記述があると、書き出す前に警告が出て確認を求められます。

### ツールチェーン

タイムゾーン、Node.js / Java / Go のバージョン、ツールの実効バージョン一覧、
**エージェント CLI の更新**（既定オフ）、Agent Fleet 自体の更新の適用。
→ [08 一歩進んだ使い方](08-advanced.ja.md#環境設定とワークスペースの作り直し)

### AWS SSM

社内の別ホストへ SSM でログインするための、プロファイル（共通設定）と SSM ホスト（個別）の登録です。
→ [08 一歩進んだ使い方](08-advanced.ja.md#社内の別ホストに入るssm)

### 内部リポジトリ

外部の git ホスティングを使わず、**テナントの中だけで完結する git リポジトリ**の作成・リネーム・削除と、
中身の参照です。clone URL は自動で認証されるので、接続の手順はありません
（ワークスペースが停止中でも操作できます）。→ [04 リポジトリと git](04-git.ja.md)

### 危険な操作

**ワークスペースを作り直す**（`~/repos` だけを消して最新イメージで作り直す）と、
**ホームを掃除する**（ログイン・接続以外のホームも消す、より深いリセット）。
どちらも未コミットの変更が消えます。
→ [08 一歩進んだ使い方](08-advanced.ja.md#ワークスペースを作り直す危険な操作)

---

## やりたいこと → どのタブか

| やりたいこと | タブ |
|---|---|
| 文字が小さい / 配色を変えたい | 表示 |
| 別のサインイン方法でも入れるようにしたい | アカウント |
| Enter で送信したい | キー操作（送信キー） |
| ペインをタブで切り替えたい | 表示（メイン領域の配置） |
| ショートカットを変えたい / 端末にキーを渡したい | キー操作 |
| 回答を読み上げてほしい | 読み上げ |
| Slack への通知だけ止めたい | 通知（サービス通知） |
| アシスタントを英語で答えさせたい | アシスタント（回答言語） |
| オペレーターに勝手に進めさせたい / 止めたい | アシスタント（自動走行・自動応答） |
| 毎回同じ前置きをプロンプトに書いている | エージェントへの指示 |
| Claude / Codex にログインしたい | エージェント |
| 課金されるモデルを選ばせたくない | エージェント（使わないモデル） |
| セッション同士で連絡させたい | エージェント（セッション間メッセージ） |
| private リポジトリを clone したい | Gitホスティング |
| インシデントの調査を手伝わせたい | 運用・監視 |
| 席を外しても進捗を受け取りたい | チャット連携 |
| 社内ツールを AI から使わせたい | MCP サーバー |
| 手元の Claude から操作したい | MCPトークン |
| トークンを何に使ったか知りたい | 使用量 |
| 変なことを覚えさせてしまった | エージェントメモリ |
| Java / Node のバージョンを変えたい | ツールチェーン |
| 別のサーバーに入りたい | AWS SSM |
| 外部に出せないコードを置きたい | 内部リポジトリ |
| 環境が壊れた | 危険な操作 |

---

仕組みを知りたい人へ: [dev/02 Console の設計](../../dev/02-console.md)
