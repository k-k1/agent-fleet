# 08. 一歩進んだ使い方 — プレビュー・外部連携・別ホスト・環境設定

> 対象: 基本操作に慣れ、もう一歩踏み込みたいメンバー。Workspace 内サービスのプレビュー、
> 手元の Claude からの遠隔操作（MCP）、社内の別ホストへのログイン（SSM）、環境設定と
> Workspace の作り直しを扱います。必要になったところだけ読めば十分です。

## サービスプレビュー — 立ち上げた Web を見る

Workspace 内で起動した Web サービス（Spring Boot・各種 dev サーバー・任意の Web アプリ）を、
**追加のポート公開もコンテナ作り直しも無し**で新しいタブから開けます。

ワークスペース操作バー右の**ポート入力欄**にポート番号を入れて **「プレビュー」** を押すと、そのポートの
サービスが新しいタブで開きます（Workspace が running のときだけ）。

- **Spring Boot** — リンクやリダイレクトを正しく解決させるには、アプリ側で `server.forward-headers-strategy=framework`（または `native`）を設定してください。
- **制約: HTTP のみ。WebSocket / SSE は未対応**です。そのため **Vite / React の HMR（ホットリロード）は動きません**。動作確認には使えますが、ライブリロード前提の開発サーバーはブラウザ側の自動更新が効かない点に注意してください。
- ポートは手入力です（自動検出はありません）。絶対パスで資産を読むアプリは 404 になることがあり、その場合はアプリ側で base path を設定します。

たとえば shell セッションで API サーバーを 8080 で起動したら、ポート欄に `8080` を入れて
「プレビュー」を押せば、新しいタブでその API を叩けます。追加のポート開放を情シスに
依頼する必要はありません。

## 外部の Claude から自分の Workspace を操作する（MCP）

手元の PC の Claude Code / Claude Desktop から、Workspace のセッションを**遠隔操作**できます。
「外出先の自分の Claude から、会社の Workspace で走っているセッションの様子を見て、
次の指示を送る」といった使い方です。そのためのトークンを **⚙設定 → 「MCP」タブ**で
発行します。

1. **名前**（例: `laptop-claude`）と **スコープ**、**有効期限**を選んで **「トークンを発行」**。
   - スコープは **read（閲覧のみ）** / **write（セッション駆動・既定）** / **admin:dangerous（強権・管理）**。あなたの権限を超えるスコープは選べません。遠隔からセッションを動かしたいだけなら write で十分です。
   - 有効期限は 90 日（既定）／30 日／365 日／無期限。
2. 発行すると **「トークンを発行しました（この画面を閉じると再表示できません）」** と出ます。**トークンはこの 1 回しか表示されません**。「トークンをコピー」で控えてください。
3. 同じ画面に、手元の Claude Code 用の **`.mcp.json`** のひな型も出ます。「.mcp.json をコピー」でコピーし、プロジェクト直下に保存（または既存ファイルに `agent-fleet` を追記）します。エンドポイントは `/mcp`、ヘッダに `Authorization: Bearer <トークン>` が入ります。

不要になったトークンは同じ画面で **「失効」** できます（失効すると、そのトークンを使う
接続は次回から弾かれます）。

## 社内の別ホストに入る（SSM）

自社 AWS の EC2 に、AWS SSM Session Manager でログインできます。設定は **⚙設定 →
「AWS SSM」タブ**で、**2 層**に分かれています。

- **プロファイル（共通設定）** — アクセスポータル（IAM Identity Center）とアカウント／ロール。複数のホストで使い回す SSO の束です。まずこれを 1 つ作ります。
- **SSM ホスト（個別）** — ログイン先の別名 → インスタンス ID。認証はプロファイルを選ぶだけです。

**AWS の秘密情報は Agent Fleet に保存されません。** ログインはセッション開始時に、
端末に出る **`aws sso login`** の URL をブラウザで承認する device-code 方式で、
短命の資格情報は Workspace 内にだけ保持されます。

登録後は、ワークスペース操作バーの **「はじめる」→「SSM — 別ホストへログイン」** で
**ログイン先ホスト**を選んで接続します。
認証が必要なら `aws sso login` の URL がモーダルに出るので、別タブで承認してください
（身に覚えのないコード／URL は入力しないこと）。

## 環境設定と Workspace の作り直し

**⚙設定 → 「ワークスペース」タブ**で、Workspace の環境を調整できます。変更は**この後に
起動するセッション／シェルに反映**されます（起動中のものと既存プロセスは Stop → Start で反映）。

- **タイムゾーン (TZ)** — 既定は日本時間。変更の**反映には Stop → Start が必要**です。
- **Node.js / Java (JAVA_HOME)** — 使うバージョンを選べます。
- **エージェント CLI の更新** — 「起動時に claude / opencode / codex を最新へ更新する」。既定は OFF（イメージに焼いた版で固定）。

### Workspace を作り直す（危険な操作）

同じタブの下部、**「危険な操作」** に **「Workspace を作り直す」** があります。コンテナを
破棄して最新イメージで作り直す操作で、**「作り直す」** を押すと確認が出ます。何が残り、
何が消えるかは以下のとおりです。

- **消えるもの** — 実行中のセッション、および **クローン済みリポジトリ（`~/repos`・未コミット変更を含む）**。
  削除されるのは `~/repos` **だけ**です。
- **残るもの** — それ以外のホーム（`~`）はすべて残ります。ログイン・接続（GitHub / Bitbucket /
  Claude など）、`~/.local`（claude / node など）、各種設定・キャッシュは、ホームボリュームが
  そのまま再アタッチされるので保持されます。

つまり「**`~/repos` だけが削除され、コンテナは最新イメージで作り直される。home のそれ以外
（ログイン・接続・`~/.local` 等）は残る**」操作です。イメージ更新を取り込みたい、
環境が壊れた、というときに使います。**未コミットの変更が消える**ので、実行前に
push / コミットを済ませてください（[04](04-git.md)）。

### ホームを掃除する（さらに深いリセット）

作り直しは `~/repos` しか消さないため、ホーム側（`~/.local` の壊れた claude 導入、
壊れたキャッシュや設定ファイルなど）が原因のときは直りません。その場合は同じ「危険な操作」に
ある **「ホームを掃除する」** を使います。**ログイン・接続を除くホーム全体**（`~/repos`・
`~/.local`・各種キャッシュ・設定）を削除して最新イメージで作り直す、より深いリセットです。

- **残るもの** — ログイン・接続（GitHub / Bitbucket / Claude）**のみ**。
- **消えるもの** — 実行中のセッション、クローン済みリポジトリ、および `~/.local`・キャッシュ・
  設定など**ホームのその他すべて**。

まず「作り直す」で直るか試し、それでも直らないときだけ「掃除する」を使ってください。
どちらも**未コミットの変更は消えます**。

---

仕組みを知りたい人へ: プレビューと MCP・SSM は [dev/08 外部連携](../../dev/08-integrations.md)、作り直しの挙動は [dev/04 Workspace Agent](../../dev/04-workspace-agent.md)
