---
audience: "全員"
source_of_truth: "この表（列と印のある行はコードと突き合わせている）"
updated: "2026-09"
---

# エージェント — 種別ごとにできること

[English](agents.md) | 日本語

セッションの種別は 11 個。うち 9 個はコーディングエージェントを動かす（か、動かす
想定）——lcpp と muse は managed 専用で Terminal（CLI）経路を一切持ちません（該当行の
脚注を参照）。`shell` と `ssm` はエージェントのいない端末です。「素の shell セッションでも
これは効くの？」は実際に問われるので、表に入れています。

✓ = 対応、— = 非対応 / 該当なし。

| 機能 | claude | codex | opencode | copilot | cursor | kiro | agy | lcpp | muse | shell | ssm |
|---|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| マネージド実行（端末なし）| — | ✓ | ✓ | ✓ | ✓ | ✓ | — | ✓ | ✓ | — | — |
| ターミナル（CLI）実行 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | —⁹ | —⁹ | ✓ | ✓ |
| 実行中のチャットミラー | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — | — |
| 停止中の履歴表示（読み取り専用）| ✓ | ✓ | ✓ | ✓ | —³ | ✓ | ✓ | ✓ | ✓¹² | — | — |
| 起動時のモデル選択 | ✓ | ✓ | ✓ | ✓¹ | ✓ | ✓ | ✓ | — | ✓¹⁴ | — | — |
| reasoning effort 指定 | ✓ | ✓ | ✓ | ✓ | —² | —⁵ | —² | — | ✓ | — | — |
| Plan モード | ✓ | ✓ | ✓ | ✓ | ✓ | — | — | ✓ | — | — | — |
| コンテキスト使用量ゲージ | ✓ | ✓ | ✓ | — | — | ✓ | — | — | —¹¹ | — | — |
| 画像貼り付け | ✓ | ✓ | ✓⁶ | — | — | — | ✓ | — | —¹¹ | — | — |
| 会話を新しいセッションへ複製 | ✓ | ✓ | ✓ | ✓ | — | — | — | ✓ | —¹¹ | — | — |
| 過去の発言からの分岐 | ✓ | ✓ | ✓ | ✓ | — | — | — | ✓ | —¹¹ | — | — |
| 権限確認のスキップを選べる | ✓ | — | — | ✓ | ✓ | ✓ | ✓ | ✓ | —¹³ | — | — |
| スキル / コマンドのピッカー | ✓ | ✓ | ✓ | —⁴ | ✓ | —⁴ | —⁴ | —⁴ | —¹¹ | — | — |
| 別セッションへの引き継ぎ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — | —¹¹ | — | — |
| git worktree で起動 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — | —¹¹ | ✓ | — |
| 定時実行（無人実行）| ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — | —¹¹ | — | — |
| チャットブリッジ（Discord / Slack）| ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — | —¹¹ | — | — |
| アシスタントチャットとして利用 | ✓ | ✓ | ✓ | — | ✓⁷ | — | ✓ | — | —¹¹ | — | — |
| 使用量 / 残量チップ | ✓ | ✓ | — | ✓ | — | — | ✓ | — | —¹¹ | — | — |
| エージェントへの指示が届く | ✓ | ✓ | ✓ | ✓ | —⁸ | ✓ | ✓ | ✓¹⁰ | —¹¹ | — | — |
| 連携（MCP）サーバーが届く | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — | —¹¹ | — | — |
| エージェントメモリを版管理できる | ✓ | ✓ | — | — | — | — | — | — | — | — | — |

¹ copilot のモデル一覧はプラン依存。Free は「Auto（Copilot が選ぶ）」のみ。

² cursor と agy は reasoning effort をモデル名に畳み込んでいるので、別のつまみが無い。

³ cursor のマネージド実行はローカルに転写を残さないため、**停止中**の cursor
セッションには表示するものがありません。実行中のミラーは動き、ターミナル（CLI）で
動かせば読める履歴が残ります。kiro は逆に、マネージドでも読める転写を残します。

⁴ ピッカーが並べるのは「CLI 自身が発見して起動できるもの」で、copilot / kiro / agy /
lcpp にはその機構が確認できていません（lcpp はそもそも CLI を駆動しないので、自前で
発見するものがありません）。リポジトリ内の別規約の `SKILL.md` ツリーに書かれた
スキルは、注入方式でこれらにも渡ります。

⁵ kiro は effort のフラグを受け付けますが、モデル毎のピッカーは出していません。

⁶ opencode の画像貼り付けは、プロバイダのキーの先にあるモデル次第。

⁷ cursor のアシスタントは読み取り専用で動きます。

⁸ Cursor は User Rules を Cursor アカウント側に持ち、ローカルにユーザー単位の
置き場がありません。**指示を受け取れない唯一の種別**です（設定の一覧には理由付きで
出ます）。

⁹ lcpp（ADR 0093 の自前 llama.cpp ハーネス）と muse（Meta の Muse Code）は、設計として
Terminal（CLI）経路を一切持ちません。lcpp はパネルに置くベンダー CLI が無く、この
プロセス自身が in-process で持つ managed runtime しかありません。muse には CLI が
ありますが、そのセッションプロトコルがパネルに映る以上のものを既に運ぶので、Agent Fleet は
そちらを駆動します。どちらの kind も、driver を指定せずにセッションを作ると、（失敗する
Terminal の起動を試みるのではなく）既定で Managed になります。

¹⁰ lcpp は指示を書き込む先の CLI を持ちません。代わりにハーネス自身が fleet／利用者
本人／プロジェクトの各指示層を読み、毎ターン system prompt に折り込みます。

## サインインの仕方

| 種別 | サインイン |
|---|:--:|---|
| claude | OAuth。ブラウザで承認し、表示されたコードを貼り戻す。接続後はアカウントのメールとプランが出る。|
| codex | ChatGPT サブスク（device code。事前に ChatGPT の「セキュリティ」で device-code 認証を有効にする）か、OpenAI の API キー。|
| opencode | 2 段構え。**「opencode を使う」**（オフのあいだは鍵もサインインも無視・既定オフ）と、**「opencode.ai への課金」**（使わない（自分の鍵だけ）/ 無料モデルのみ / Go（定額）/ Zen（従量））。後者は opencode.ai の使い方だけを決め、自分でつないだプロバイダのモデルはどれを選んでも残る。キーは使いたい LLM プロバイダの API キーを env として保存（プリセットで env 名が入る・複数登録可）。|
| copilot | 専用のサインインは無い。**GitHub を git プロバイダとして接続すると接続済みになり**、GitHub を切ると一緒に切れる。そのアカウントに Copilot サブスク（Free 含む）が要る。|
| cursor | 承認リンクをブラウザで開いて承認するだけ（**貼り戻すコードは無い**）。Cursor アカウントが必要で、API キーでは接続できない。|
| kiro | device flow。確認コード付きのリンクを開いて承認（Builder ID / Google / GitHub 等）。API キーでは接続できない。CLI が大きいので、焼き込んでいない配備では初回にオンデマンド導入される。|
| agy | エージェント設定のカードからサインインする。|
| shell | 該当なし。|
| ssm | ワークスペースの AWS SSM 接続を使う。|

## ミラーに出る状態

| 種別 | 状態 |
|---|:--:|---|
| claude | 進行中 / 質問あり / プランあり / 許可待ち / 入力待ち |
| codex | 進行中 / 質問あり / プランあり / 入力待ち |
| opencode | 進行中 / 質問あり / 入力待ち |
| copilot | 進行中 / 許可待ち / 入力待ち |
| cursor | 進行中 / 入力待ち |
| kiro | 進行中 / 許可待ち / 入力待ち |

`shell` と `ssm` は会話が無いので状態モデルも通知もありません。agy の状態は個別に
文書化されていません——ミラーは best-effort と考えてください。

## どれを選ぶか

**すでに払っている契約で選ぶ**のが確実です。Anthropic のアカウントなら **claude**、
ChatGPT や OpenAI API なら **codex**、複数プロバイダの API キーを切り替えたいなら
**opencode**、GitHub Copilot のサブスクがあるなら **copilot**、Cursor のプランがあるなら
**cursor**、AWS Builder ID（や Kiro プラン）なら **kiro**。

どれも会話表示・ミラーからの回答・他エージェントへの引き継ぎに対応します。実際に
決め手になりやすい差は、コンテキストゲージ（claude / codex / opencode / kiro）、
画像貼り付け、そしてマネージド実行が要るかどうか——**Codex と opencode は
セッション毎のプロセスを持たない**ので、数を並べても重くなりません。

## lcpp: 実機計測で分かったこと

`lcpp`（ADR 0093）はベンダーのホスト型 API ではなく、自前の llama.cpp エンジンの
インスタンスを動かします。16 回の実機走行で、最初のセッションの前に知っておくべき
実際の費用が分かりました。

- **最初のターンは分単位で待ちます。** エンジンのインスタンスが止まっていると、
  真のコールドスタートに**おおよそ 3.5〜7 分**かかります（多くは 4〜5 分帯に収まり、
  1 件は 302 秒でタイムアウトしました）。セッションが固まっているのではなく、
  インスタンスを購入して起動している時間です。2 回目以降のターンは、インスタンスが
  温かいままなら速く返ります。
- **文脈窓は 8000 トークン以上を選んでください。** 窓 3500 では、一部のモデル
  ファミリーが圧縮を連続で発火させ、ハーネスが無限に回り続ける代わりに意図的に
  停止します——Gemma と Qwen3-Coder の両方で再現しました。窓 8000 ではどちらも
  問題なく完走し、窓 24000 では圧縮自体が起きません。この停止は設計どおりの
  動作であり、不具合ではありません。
- **同じ課題でもファミリーによってターン数が大きく違います。** 同じ窓でも
  費用差は数倍に及びます。ある実測の課題（小さなプロジェクトの 5 か所の
  不具合修正＋想起の質問 1 問）では、Qwen3.8 が 29〜30 ターン、Gemma-4 が 63
  ターン、GPT-OSS が 95 ターン、Qwen3-Coder が 164 ターン（Gemma-4・Qwen3-Coder
  はどちらも窓 8000）でした。4 つとも正しく完走しており、差は正しさではなく
  費用です。
- **動作を確認したモデルファミリーは Qwen3・GPT-OSS・Gemma の 3 つ**（4
  チェックポイントで確認）。`llama-3.1-8b-instruct-q4_k_m` は**動きません**:
  このエンジンのビルドには Llama 専用の tool call パーサが無く、tool call の
  構文解析が失敗します。目録にあるモデルがどれも同じように動くわけではないので、
  確認済みのファミリーを選んでください。
- **モデルの入れ替えにインスタンスの入れ替えは要りません。** 最初の 1 回を除き、
  同じエンジン上で別のモデルへ切り替えてもコールドスタートは発生しませんでした。
  高くつくのは最初の購入だけです。

`lcpp` には Terminal（CLI）経路が一切ありません——上の脚注 9 のとおりで、動くのは
Managed だけです。

## この表に無いもの

- **Rovo Dev** はさらに別の種別として調査しましたが、実装していません。
- ドライバのデバッグ中にしか効かない種別ごとの癖は `docs/build/` へ。

> エージェントはあなたの代わりにコマンドを実行し、ファイルを編集し、push します——
> 定時実行では無人で、権限確認をスキップするモードでは都度尋ねずに。`shell` と `ssm`
> は送った文字列を**そのまま**実行します。バックアップを取り、最小権限の資格情報を使い、
> 承認ゲートに頼ってください。

¹¹ muse はまだ構築中です（ADR 0095）。起動メニューには**出るようになりました**が、出るのは
次の 2 つが揃ったときだけです: Muse Code はプロプライエタリでイメージに含まれないので
オンデマンドで導入する必要があり（Muse Code の接続カードから・約 299MB がホームに入ります）、
かつサインイン済みである必要があります——未認証のセッションは仕事を受け取ったうえで毎ターン
失敗します。この脚注が付いた行がこれから載る部分で、各々が着地するたびに埋まっていきます。

¹² Agent Fleet は muse の会話を進行中に自分の側へ書き写すので、停止中のセッションでも
履歴が見えます。Muse Code 自身のセッションファイルは読める転写ではなく内部形式の実行時
ログなので、ここでは出典になりません。

¹³ 🔴 muse のセッションはツールの承認を一度も求めません。スキップする対象が無いので選べない
——そしてこの行の「—」は、機能が無いという意味ではなく**安全側が薄い**という意味です。Muse Code
自身のサンドボックスは Workspace のコンテナの中では構築できず（コンテナのセキュリティプロファイル
が拒否します）、そのサンドボックスを切るフラグはセッションのファイルシステムとローカル網の制限も
一緒に外します。結果、どのツール呼び出しも承認を考える前にポリシーで許可されます。実測では、muse
のターンが作業コピーの外にあるファイルを確認なしで書きました。よって muse のセッションはこの
コンテナに対して `shell` と同じ届き方をするものとして扱い、上の警告はそのまま当てはまると読んで
ください。

¹⁴ Muse Code の一覧には、どのモデルにも「-contributor」版が並びます。モデルも料金も同じですが、
そちらは会話（セッション間のメッセージを含む）が Meta の製品改善に使われることがあります。しかも
Muse Code 自身の既定がそれです。Agent Fleet は代わりに選んだりはしません——**既定**のまま起動した
セッションは、その条件が付かない最新のモデルで動き、選びたい人のために contributor 版もピッカーに
残してあります。選ぶ場所は 設定 › エージェント › Muse Code › 動作 で、reasoning effort（`none`〜
`ultra`）も同じところにあります。
