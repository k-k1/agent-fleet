---
audience: "エージェントを選んで接続する人"
updated: "2026-09"
---

# 06. エージェント — claude / codex / opencode / GitHub Copilot / Cursor / Kiro の接続と選び方

[English](06-agents.md) | 日本語

接続はすべて **⚙設定 → 「エージェント」タブ**から行います（ワークスペースの起動が必要）。

## 対応エージェントと選び方

主要 6 つの CLI コーディングエージェントに対応しています（実験枠の Antigravity（agy）は
[08](10-integrations.ja.md)。マネージド専用の lcpp と muse は下の機能一覧表に掲載）。
接続の変更は即時、挙動設定は各エージェントの**新しいセッションから**反映されます。

| | claude | codex | opencode | copilot | cursor | kiro |
|--|--------|-------|----------|---------|--------|------|
| 認証方式 | OAuth 接続（コード貼付） | ChatGPT サブスク / API キー | プロバイダの API キー（env） | GitHub 連携に相乗り（個別ログイン不要） | Cursor アカウントにサインイン（ブラウザ承認のみ） | device-flow サインイン（Builder ID / Google / GitHub — ブラウザ承認のみ） |
| 起動時のモデル選択 | あり | あり | あり | あり（プラン依存 — Free は自動のみ） | あり（アカウント連動） | あり（Free でも named モデル可） |
| 状態 | 進行中 / 質問あり / プランあり / 許可待ち / 入力待ち | 進行中 / 質問あり / プランあり / 入力待ち | 進行中 / 質問あり / 入力待ち | 進行中 / 許可待ち / 入力待ち | 進行中 / 入力待ち | 進行中 / 許可待ち / 入力待ち |
| チャット表示・履歴 | あり | あり | あり | あり | 実行中: あり（ツール出力は簡略）。停止中: マネージドは履歴なし | あり（マネージドでも読み取り可能な履歴あり） |
| Planモード | あり | あり | あり | 起動時指定＋マネージド設定から切替 | あり | 未対応 |
| 実行方式 | ターミナル（CLI） | マネージド（既定）/ ターミナル（CLI） | マネージド（既定）/ ターミナル（CLI） | マネージド（既定）/ ターミナル（CLI） | マネージド（既定）/ ターミナル（CLI） | マネージド（既定）/ ターミナル（CLI） |
| 再開 | 可（作業フォルダ消失時は不可） | 可（作業フォルダ消失時は不可） | 可（作業フォルダ消失時は不可） | 可（作業フォルダ消失時は不可） | 可（実行方式をまたぐ再開は不可） | 可（作業フォルダ消失時は不可） |
| 引き継ぎ（handoff） | あり | あり | あり | あり | あり | あり |
| 画像貼り付け | あり | あり | あり（モデル依存） | 未対応 | 未対応 | 未対応 |
| 利用上限リセット後の自動再開 | あり | マネージドのみ | 未対応 | 未対応 | 未対応 | 未対応 |

迷ったら、利用している契約やモデルで選べます。Anthropicのアカウントを使うなら **claude**、
ChatGPTやOpenAI APIを使うなら **codex**、複数プロバイダのAPIキーを切り替えたいなら
**opencode**、GitHub Copilot のサブスクリプションを持っているなら **copilot**、
Cursor のプランを持っているなら **cursor**、AWS Builder ID（や Kiro プラン）を使うなら
**kiro**、という
選び方になります。いずれも会話表示、質問への回答、他エージェントへの
会話の引き継ぎに対応します（コンテキストゲージは claude / codex / opencode / kiro / lcpp / muse）。

Codex / opencode / copilot / cursor / kiro の**マネージド実行**は、会話画面から普段の操作を完結できます
（Codex / opencode はセッションごとの追加プロセスを持たないため並行作業向き。copilot / cursor / kiro は
マネージドでもセッションごとの専用プロセスで動きます）。CLI 固有の黒い画面が必要なときだけ
**ターミナル（CLI）**を選びます。詳しくは
[02 セッション](02-sessions.ja.md#実行方式-マネージドとターミナルcli)を参照してください。
マネージドのチャット表示は、左ペインのリポジトリを使わないアシスタントチャットとは別です。

接続がうまくいったかは、⚙設定 → 「エージェント」タブの各カードで確認できます。
**「接続済み」** と表示され、claude / codex ならログインしているアカウント（メール）と
プランも出ます。1 つ接続すれば、新規セッションの種類にそのエージェントが並ぶように
なります（[02](02-sessions.ja.md)）。

## 機能対応の早見表（全種別）

冒頭の表は主要 6 CLI エージェントの比較です。こちらは Antigravity（agy）・マネージド専用の
lcpp と muse・エージェント以外のセッション種別（shell / SSM）を加え、本ガイドの他章で扱う
横断的な機能——worktree（[04](03-code.ja.md)）、定時実行とチャットブリッジ
（[11](08-organising.ja.md)、[08](10-integrations.ja.md)）——もまとめたものです。
✓ = 対応、— = 非対応 / 該当なし。

| 機能 | claude | codex | cursor | copilot | kiro | agy | opencode | lcpp | muse | shell | ssm |
|---|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| マネージド（paneless）実行 | — | ✓ | ✓ | ✓ | ✓ | — | ✓ | ✓ | ✓ | — | — |
| ターミナル（CLI）実行 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | —⁴ | —⁴ | ✓ | ✓ |
| 実行中のチャットミラー | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — | — |
| 停止中の履歴表示（読み取り専用） | ✓ | ✓ | —³ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — | — |
| 起動時のモデル選択 | ✓ | ✓ | ✓ | ✓¹ | ✓ | ✓ | ✓ | ✓ | ✓ | — | — |
| reasoning effort 指定 | ✓ | ✓ | —² | ✓ | — | —² | ✓ | — | ✓ | — | — |
| Plan モード | ✓ | ✓ | ✓ | ✓ | — | — | ✓ | ✓ | — | — | — |
| コンテキスト使用量ゲージ | ✓ | ✓ | — | — | ✓ | — | ✓ | ✓ | ✓ | — | — |
| 画像貼り付け | ✓ | ✓ | — | — | — | ✓ | ✓ | — | ✓ | — | — |
| 会話の引き継ぎ（handoff） | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — | ✓ | — | — |
| git worktree で起動 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — | ✓ | ✓ | — |
| 定時実行（無人実行） | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — | ✓ | — | — |
| チャットブリッジ（Discord / Slack） | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — | ✓ | — | — |
| アシスタントチャットとして利用可 | ✓ | ✓ | ✓ | — | — | ✓ | ✓ | — | ✓⁵ | — | — |
| WSバーの使用量 / 残量チップ | ✓ | ✓ | — | ✓ | — | ✓ | — | — | ✓ | — | — |

¹ copilot のモデル選択はプラン依存です（Free は Auto のみ）。

² cursor と agy は reasoning effort をモデル名に内包するため、別途の指定はありません。
kiro は `--effort` フラグを受け付けますが、モデル別の effort ピッカーは出しません。

³ cursor はマネージド（既定）実行ではローカルに転写を残さないため、**停止中**の cursor
セッションには表示できる履歴がありません（実行中のミラーは動作し、ターミナル（CLI）で
実行した場合は読み取り可能な履歴が残ります）。一方 kiro はマネージドでも読み取り可能な
転写を残すため、停止中の kiro セッションでも履歴が表示できます。

⁴ lcpp と muse はターミナル（CLI）経路を持ちません——lcpp はパネルに置くベンダー CLI が
なく、muse はセッションプロトコルを直接駆動します。どちらも既定でマネージドになります。

⁵ アシスタントとしての muse は 1 ターンを 1 回のヘッドレス実行で答え、会話を覚えています。
「答える」だけで「する」ことはありません（このターンではシェル・ファイル書き込み・Web の
ツールが切られています）。先に Muse Code の導入とサインインが要ります——下の接続カードを
見てください。muse の行はすべて、実際の muse セッションで動くのを確認してから ✓ にしています。

使用量チップが増えると WSバーが狭くなるため、**バーに残るのは直近に使った 2 つだけ**です。
残りは右の **「+N」** に畳まれ、押すと中で開けます（畳まれていても値は取り続けるので、
開いた瞬間に取り直すことはありません）。上限に近づいた（95% 以上／Full reset を持つ）
チップは、しばらく使っていなくても表に出ます。各チップのドロップダウンの
**「WSバーでの表示」** で、そのエージェントを **常に出す**／**自動**（既定）／
**常に畳む** から選べます——3 つ目を常に出したいときは「常に出す」を選んでください
（「常に畳む」は上限に近づいても表に出しません）。スマホでは ⋯ の中に縦に並ぶため、
畳みません。

WSバーの使用量チップはアカウント単位の残量枠がある場合に表示されます——opencode
（各自のプロバイダ API キー）・cursor・kiro にはその枠がありません。**lcpp** と **muse**
はターミナル（CLI）経路を持たず、どちらも既定でマネージドになります。**muse** は起動
メニューに出るまでに Muse Code のオンデマンド導入（約 299 MB）とサインインが必要です。
🔴 muse のセッションはツールの承認を一度も求めません——承認を検討する前にすべての
ツール呼び出しがポリシーで許可されます。このワークスペースへの届き方は `shell` と同じと
考えてください（上の自律実行の注意書きはそのまま当てはまります）。**shell** は素のシェル、
**ssm** は AWS SSM 経由のリモートログインで、どちらもターミナルのみ——会話・状態モデル・
通知はありません。

**アシスタントチャットの既定モデル** — 各アシスタントで個別にモデルを指定でき、claude の
既定はデプロイ全体で `AF_CHAT_MODEL` でも変更できます。会話用途のため高速・低コストの
ティアを既定にしています: claude → Sonnet 5 · codex → `gpt-5.6-luna` · opencode →
`opencode/nemotron-3-ultra-free` · agy → Gemini 3.5 Flash · cursor → Cursor 自身の既定
（Auto）。なお cursor のアシスタントは**読み取り専用**（`--mode ask`）です。kiro は
アシスタントチャットとしては**利用できません**（headless チャットを持たないため）。

> **自律実行についての注意。** エージェントは利用者に代わってコマンドを実行し、ファイルを
> 編集し、push します——無人（定時実行）でも、権限確認を省くモードでは毎回尋ねずに実行する
> こともあります。shell / SSM セッションは送った文字列を**そのまま**実行します。これらは
> 破壊的・不可逆になりえます。バックアップを保持し、最小権限の認証情報を使い、承認ゲート
> （shell コマンドの確認、チャットブリッジの承認 / 却下）を活用してください。
> あわせて [08 フリートオペレーター](08-organising.ja.md) も参照してください。

## Claude

「エージェント」タブの **Claude** で **「OAuth 接続」** を押すと、サインインが新しいタブで
開きます。自分のブラウザで承認し、表示された**コードを貼り付けて「完了」** します
（タブが自動で開かないときは「サインインリンク ↗」から開けます）。接続済みになると
メールとプラン（例: `…@gmail.com · pro`）が表示されます。

Claude の挙動は同じ画面で切り替えられます。

- **既定モデル** — claude セッションを起動するときの初期選択モデル。Opus / Sonnet / Haiku などのティア別名はそのティアの最新版に追従し、登録した完全モデル ID は特定のリリースに固定します。
- **Claude の追加モデル** — `claude-opus-4-8` のような完全 ID を登録すると、旧リリースが起動ダイアログ・既定モデル設定・MCP `list_models` の通常の選択肢になります。Claude Code の OAuth サブスクリプションにはアカウント連動のカタログ取得口がないため、利用できるかはセッション起動時に Claude Code が確認します。登録を削除しても既存セッションの設定は書き換えません。
- **使わないモデル** — 選ばせたくないモデルを外します。外したモデルは起動ダイアログ・設定・アシスタントが選ぶ一覧（MCP `list_models`）から消え、明示的に指定した起動（定時実行のモデル欄やアシスタント経由も含む）は断られます。プランによって追加課金になるモデル（例: Claude の Team プランの Fable は API クレジット扱い）の誤選択を防ぐための設定です。エージェントごとに設定でき、除外した時点で「既定モデル」やリポジトリごとの前回値も自動で外れます。除外は選んだモデル 1 つだけに効きます（`gpt-5.4` を外しても `gpt-5.4-mini` は別モデルとして残ります）。ただし claude のティア名（`fable` など）は別名なので、その別名を含む完全なモデル名にも効きます。なおターミナル内で `/model` を打つなど CLI 自身の操作までは止められません——これは選択の事故防止であって課金の強制ガードではありません。
- **思考を展開して表示** — セッション画面の「思考」ブロックを最初から開いた状態で出します。既定はオフ（畳んだ状態で、見出しをクリックすると読めます）。claude の思考は、ツールを次々に走らせている間に「いま何をしているか」を短くまとめた文章で、ターミナルに出ているものと同じです。長い自動作業の途中では、これが唯一の文章になることもあります。表示だけの設定で、エージェントの動き方は変わりません（codex / opencode 側の同じ設定とは独立です）。
- **リモートコントロール** — 手元の Claude アプリなどから、走っているセッションを遠隔操作する機能のオン / オフ。新しいワークスペースでは既定オフ（必要ならここでオンにします）。
- **通知** — セッションの状態変化を通知するかどうか。
- **RTK（トークン節約）** — 後述。

> **「Select login method」やログイン画面が出てしまうときは** → セッション側の
> 一時的な状態で、接続そのものは生きていることがほとんどです。対処は
> [11 困ったとき](11-troubleshooting.ja.md) を見てください。端末内で手動 `/login` する
> 従来のやり方も併用できます。

## Codex

**Codex** は 2 通りで接続できます。

- **ChatGPT サブスクで接続**（推奨）— Plus / Pro の枠を使い、追加課金なし。デバイスコード方式です。事前に **ChatGPT の「設定 > セキュリティ」で「Codex に対してデバイスコード認証を有効にする」をオン**にしておく必要があります（これがオフだと承認しても進みません）。
- **API キーで接続** — OpenAI API の従量課金（`sk-…`）。

接続の流れは GitHub と同じ 3 ステップ（コードをコピー → リンクを開いて貼り付け →
承認を待つ）です。

**動作設定**には、codex の思考（chain-of-thought）の見せ方もあります。

- **思考を展開して表示** — セッション画面の「思考」ブロックを最初から開いた状態で出します。既定はオフ（畳んだ状態で、見出しをクリックすると読めます）。表示だけの設定で、エージェントの動き方は変わりません。エージェントごとに独立していて、同じ設定が claude / opencode 側にもあります。

## OpenCode

カードには 2 つのコントロールがあり、決めていることが違います。

**「opencode を使う」** はエージェントぜんぶの電源です。**オフ**のあいだ opencode は起動
しません——API キーを保存していても、アカウントにサインインしていても、あとから鍵を足しても
です。新しいワークスペースはこの状態から始まるので、自分で選ぶまで opencode.ai には何も
つながりません。

**「opencode.ai への課金」** はオンにすると出てきて、**opencode.ai の使い方だけ**を決めます。
どれを選んでも、自分でつないだプロバイダ（Anthropic・OpenRouter など）とフリートの自前
エンジンのモデルは起動一覧に残ります。

| 選択 | 起動一覧に出るもの | 必要なもの |
|---|---|---|
| **使わない（自分の鍵だけ）** | 自分のプロバイダと自前エンジンだけ。`OPENCODE_API_KEY` は保存されていても注入しません。 | 自分のプロバイダキー 1 本（または自前エンジン） |
| **無料モデルのみ** | 認証なしで使える opencode.ai のモデル。混雑と上限に左右されます。 | 不要 |
| **Go（定額）** | サブスクの id（`opencode-go/…`）。 | API キー（サインインは任意） |
| **Zen（従量）** | 従量課金の id（`opencode/…`）。Go 契約もあれば両方。 | アカウントのサインインか API キー |

選んだ枠のモデルが 1 つも無いとき（Go 契約が無いのに Go を選んだ場合など）、起動一覧は Zen
に落ちます。**落ちたことはカードが言います**——意図した枠で課金したいときは、認証か契約を
確認してください。

**API キー**は、プリセットから選ぶと env 名が自動で埋まります。

- **opencode.ai**（`OPENCODE_API_KEY`・Go と Zen で同じキー）/ **Anthropic** / **OpenAI** / **OpenRouter** / **Google Gemini** / **Sakana AI**（`SAKANA_API_KEY`・Fugu / Fugu Ultra）/ **カスタム…**（env 名を自分で指定）

キーを貼って **「接続」** すると保存され、opencode起動時に注入されます。複数のキーを
登録でき、起動時に接続済みプロバイダのモデルを選べます。いまの枠が注入しないキーは、消さずに
「この枠では注入しません」と添えて残ります。

キーや課金の選択を変えても、**稼働中の opencode serve には自動では反映されません**
（serve は起動したときの設定のまま動きます）。反映が要る変更をすると、カードに
「再起動するまで反映されません」という案内と **「opencode serve を再起動して反映」**
ボタンが出ます。再起動すると、応答の途中だった opencode セッションは区切りを待ってから
中断されるので、手が空いているときに押してください。

**動作設定**では、opencode の思考（chain-of-thought）の見せ方も選べます。

- **思考を展開して表示** — セッション画面の「思考」ブロックを最初から開いた状態で出します。既定はオフ（畳んだ状態で、見出しをクリックすると読めます）。表示だけの設定で、エージェントの動き方は変わりません（claude / codex 側の同じ設定とは独立です）。

## GitHub Copilot

**copilot**（GitHub Copilot CLI）に個別のログインはありません。**git プロバイダの
GitHub を連携すると自動的に「接続済み」になります**（「Gitホスティング」タブ > GitHub。切断も GitHub
側に連動します）。前提として、その GitHub アカウントに **Copilot のサブスクリプション**
（Free プラン含む）が必要です — 無い場合は最初の指示でエラーになります。

- 起動時のモデル選択肢は**プランに応じて自動で切り替わります**。Free プランは
  「自動（Copilot が選択）」のみ、有償プランではそのアカウントで選べるモデルが並びます。
- Free プランは月間の利用枠が小さめです。使用量は GitHub 側の設定画面で確認できます。

## Cursor

**cursor**（Cursor CLI）は「エージェント」タブの **Cursor** カードで
**「Cursor にサインイン」** を押します。認可リンクが表示されるので、ブラウザで開いて
承認するだけです（**貼り付けるコードはありません** — 承認するとカードが自動で
「接続済み」になります）。前提として Cursor のアカウントが必要です。API キーでの接続は
未対応です。

- 起動時のモデル選択肢は、**そのアカウントで使えるモデルがそのまま並びます**
  （ライブ取得）。セッション起動後のモデル変更はできません。
- 使用量チップ・コンテキストゲージ・画像貼り付けは cursor では
  未対応です。プラン残量は Cursor のダッシュボードで確認してください。

## Kiro

**kiro**（Kiro・旧 Amazon Q Developer CLI）は「エージェント」タブの **Kiro** カードで
**「Kiro にサインイン」** を押します。**device-flow** のサインインで、確認コード付きの
認可リンクが表示されるので、ブラウザで開いて承認します（Builder ID / Google / GitHub
など）。承認するとカードにアカウントのメールとともに「接続済み」が表示されます。API キーでの
接続は未対応です。

- **オンデマンド導入。** Kiro の CLI は大きい（~855MB）ため、既定では**イメージに焼き込まれ
  ません**。初めて使うときに各自の home ディレクトリへダウンロードされます——接続カードに
  進捗付きの **「インストール」** ボタンが出てから、サインインへ進みます
  （`BAKE_AGENT_CLIS=1` を立てたデプロイでは同梱済みです）。
- 起動時のモデル選択肢はライブ取得で、**Free プランでも named モデルが選べます**
  （Auto、Claude Sonnet / Haiku ほか）。reasoning effort の別ピッカーはありません。
- **マネージド（既定）** と **ターミナル（CLI）** の両方に対応します。cursor と違い、
  **停止中**の kiro セッションでも読み取り可能な履歴が表示でき、実行中はライブの
  **コンテキストゲージ**が出ます。
- 画像貼り付け・Plan モード・WSバーの使用量チップは kiro では未対応で、左ペインの
  アシスタントチャットとしては利用できません。

## Muse Code

**Muse Code** はマネージド専用の種別です。起動メニューに出るには 2 つが揃っている必要が
あります:

1. ⚙設定 → 「エージェント」の **Muse Code** カードで **「Muse Code を導入」** を押します（約 299 MB、
   ホームへ 1 回だけ。終わると自動でサインイン画面になります）。ピンされた新しい版があるとカードが
   そう言い、**「Muse Code を更新」** が出ます——押すまでは導入済みの版が使われ続け、稼働中の muse
   セッションは再起動するまで古い版で動きます。
2. その後サインインします。**「Meta アカウントでサインイン」** は承認リンクとコードを表示するので
   ブラウザで承認します——貼り戻すコードはありません。サブスク契約ならこちらです。**「API キーを使う」**
   は従量課金の経路で、キーを保存すると保存済みのアカウントのサインインは消えて従量課金になるため、
   アカウントでサインイン済みなら先に切断してください。

同じカードの **「動作」** 設定でモデル（Agent Fleet は既定で「-contributor」条件なしの最新
モデルを選択します——「-contributor」モデルの意味は [エージェントリファレンス](../ref/agents.ja.md) を参照）と
reasoning effort を指定できます。

稼働中の muse セッションでは、入力欄の横の **`/`** ボタンがそのセッション自身のスキル——Muse Code
同梱のもの・プラグインのスキル・`~/.config/muse/skills` にある自分のもの・作業コピーの
`.agents/skills/`——を並べ、選ぶとそのスキルが走ります（[07](07-chat-memo.ja.md#スキルやコマンドを呼び出す)）。

> 🔴 **muse のセッションはツールの承認を一度も求めません。** 承認を検討する前にすべての
> ツール呼び出しがポリシーで許可されます——Workspace のコンテナ内ではサンドボックスを
> 構築できないためです。muse のセッションはこのワークスペースへの届き方が `shell` と
> 同じと考えてください。

Agent Fleet からは見えない muse セッション内の動作（自前の定時実行・セッション間
メッセージ・セッション一覧）の詳細は [エージェントリファレンス](../ref/agents.ja.md#muse-agent-fleet-からは見えないもの) を参照してください。

## lcpp

**lcpp** はフリート自前の llama.cpp エンジンです——サインインも別途のインストールも不要で、
セッション起動ダイアログから他の種別と同じように起動できます。

⚙設定 → 「エージェント」のカードには 2 つの操作があります。**「llama.cpp を使う」**（既定はオン）が
電源で、オフにすると起動導線から消え、ほかの経路からの起動も拒まれます（稼働中のセッションは
動き続けます）。**「自分の接続先」**は、配備のエンジンの代わりに自分のネットワークにある
llama-server へセッションを向けます。URL（と、要るなら API キー）を入れて **「接続を確認」** を押すと
build・窓・モデルが出て、起動ダイアログのモデル一覧はそのサーバーのものになります。設定して
いるあいだ lcpp のセッションはそこへ直結し——テナント管理者のエンジン可否は効きません——
画面最上部の**チャット**のピルも配備のエンジンでなくその接続先（**接続中**／**届いていません**／
**確認中**と、見つかったモデル）を報告します（[バッジ](badges-and-menus.ja.md#画面最上部のエンジンのピル)）。
欄を空にすると配備のエンジンに戻ります。

最初のセッションの前に知っておくべきこと:

- **最初のターンは分単位で待ちます。** エンジンが止まっていると、真のコールドスタートで
  おおよそ 3.5〜7 分かかります。インスタンスが温かいままなら 2 回目以降は速く返ります。
- **文脈窓は 8000 トークン以上を選んでください。** 窓が小さいと、一部のモデルで
  圧縮ループが連続し、ハーネスが意図的に停止します。
- **確認済みのモデルを使ってください。** Qwen3・GPT-OSS・Gemma は動作を確認済みです。
  `llama-3.1-8b-instruct` は動きません（このビルドに Llama 専用の tool-call パーサがない
  ため）。

モデル別ターン数の実測値・コールドスタート時間・窓の根拠を含む詳細は
[エージェントリファレンス](../ref/agents.ja.md#lcpp-実機計測で分かったこと) を参照してください。

## コンテキスト残量を見る

claude / codex / opencode / kiro / lcpp / muse のセッションでは、画面上部に **「コンテキスト」**（狭いときは
`ctx`）のゲージが出ます。今の会話が使っているトークン量と上限、そのうちキャッシュ再利用・
新規キャッシュ・未キャッシュの内訳は、表示にマウスを重ねると確認できます。上限に近づくと
「まもなく自動圧縮される可能性があります」と警告が出ます。長い作業で「急に文脈が
薄くなった」と感じたら、ここを見てください。チャット表示ではターンごとの
トークン消費推移も見られます。

## RTK（トークン節約）

claude / codex / opencode / GitHub Copilot / agy の 5 エージェントに **「RTK（トークン節約）」** の
オン／オフ設定があります（cursor と Kiro には未対応です）。エージェントが
実行するコマンドを賢く書き換えて、消費トークンを抑える仕組みです。このワークスペースの
イメージに RTK が含まれていない場合は「このワークスペースに rtk がありません」と表示されます。

効き方はエージェントで少し違います。

- **claude / opencode** — コマンドを透過的に書き換えるので、意識せず効きます。
- **copilot** — フックでシェルコマンドを rtk 経由に書き換えるため、claude / opencode と同じく決定的に効きます（新規セッションから適用）。
- **codex / agy** — コマンド書き換えの仕組みを持たないため、**指示ベース（ベストエフォート）**です。「rtk を使ってね」と促すだけで、強制ではありません。

## エージェントへの指示（自分の働き方を一度だけ書く）

**⚙設定 → 「エージェントへの指示」** に書いた文章は、このワークスペースで新しく起動する
エージェントの指示に加わります。報告の言語や口調、確認を取ってほしい場面、使ってほしいツールなど、
**毎回プロンプトに書いていること**をここへ移すと、以後は書かずに済みます。

指示には 3 つの層があり、この設定は**真ん中**にあたります。

| 層 | 誰のものか | 変えられるか |
|----|-----------|-------------|
| ワークスペースの方針 | フリート全体（運用者） | ここからは変えられません（衝突したらこちらが優先） |
| **エージェントへの指示** | **あなた個人** | **この設定** |
| リポジトリの指示（`CLAUDE.md` / `AGENTS.md`） | チーム全員（コミットされる） | リポジトリを編集 |

- **リポジトリにはコミットされません。** 同僚には影響しない、あなただけの設定です。
- **新しく起動するセッションから**効きます。実行中のセッションは、起動時に読んだ内容のままです。
- 配れる先は claude / codex / opencode / GitHub Copilot / agy / Kiro / lcpp / muse です。
  **対応していないのは Cursor だけ**で、設定画面にも行として出て理由が書かれます（Cursor は
  指示の置き場がユーザー単位ではローカルに無く、User Rules は Cursor アカウント側にあるため）。
- 行ごとに**どのファイルに書かれたか**と**いま効いているか**が出ます。「保存できたが効いていない」
  ときは理由がその行に出ます。
- 長さの上限があります。ここに書いた内容は**毎セッションのコンテキストに毎回乗る**ので、
  短いほどよく効きます。
- **秘密（API キーやトークン）は書かないでください。** 複数のエージェントが読む平文です。
