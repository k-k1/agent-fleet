AWSTemplateFormatVersion: "2010-09-09"
Description: >-
  agent-fleet ECS adapter — the fleet's own inference engines on GPU, normally scaled to
  zero (ADR 0071 P0: the `llm` role, llama.cpp; P1: the `image` role, stable-diffusion.cpp's
  sd-server). Optional, and each role is optional on its own: a deployment that does not want
  self-hosted inference deploys nothing and no engine model appears in any launch menu.
  Imports the VPC, private subnets and the CP security group from 00-network, and the
  cluster, execution role, Cloud Map namespace and the af-llamacpp / af-sdcpp ECR
  repositories from 20-platform; it does NOT depend on 30-ingress. Deploy it BEFORE
  30-ingress and hand 30-ingress this stack's EnginesSsmParam output.

# ⚠️ Why Managed Instances rather than Fargate, five things this template had to learn the hard
# way on a real cluster, and one rule about who owns the cluster's capacity-provider list:
# PARAMETERS-60-engines.md, "What this template learned the hard way". Read it before editing
# the capacity providers or the services.

Parameters:
  # ⚠️ The long-form reasoning for these — the measurements, the trade-offs, the traps — is in
  # cfn/PARAMETERS-60-engines.md. It lives there rather than here because a template body over
  # 51,200 bytes cannot be handed to CloudFormation directly, and a YAML comment costs exactly
  # what a Description does.
  #
  # ⚠️ Write every parameter in BLOCK form. The one-line `{ Type: …, Default: … }` map reads as
  # REQUIRED to standup.sh's preflight — it looks for a `Default:` on its own line — and one of
  # those stops every stand-up with "required parameter … is missing from params/60-engines"
  # (measured against the stub test).
  NetworkStackName:
    Type: String
    Default: af-ecs-network
    Description: 00-network stack to import the VPC, private subnets and the CP SG from.
  PlatformStackName:
    Type: String
    Default: af-ecs-platform
    Description: >-
      20-platform stack to import the cluster, execution role, namespace and the af-llamacpp
      and af-sdcpp ECR repositories from.
  ServiceConnectNamespace:
    Type: String
    Default: af.internal
    Description: >-
      The private DNS namespace's NAME, which must match what 20-platform was given: the
      namespace is imported by id, while the engine URLs are built from the name.
  EnginesSsmParamName:
    Type: String
    Default: /af-ws/engines
    AllowedPattern: "^/af-ws/[A-Za-z0-9_.\\-/]+$"
    Description: >-
      SSM parameter this stack writes the engine table to, and the one value 30-ingress is
      handed. MUST be under /af-ws/ — that is the whole of the CP task role's SSM read scope.

  # --- the llm role (llama.cpp) -------------------------------------------------
  LlmEnabled:
    Type: String
    Default: ""
    AllowedValues: [ "", "true", "false" ]
    Description: >-
      Create the llm role at all. "" (the default) = whatever LlmModelS3Key said, which is the
      one release of compatibility ADR 0072 decision 1 allows. WHAT it loads is the catalogue's.
  LlmDiscoveryName:
    Type: String
    Default: llm
    Description: DNS label the llm engine answers on, i.e. <LlmDiscoveryName>.<namespace>.
  LlmImageTag:
    Type: String
    Default: server-cuda
    Description: >-
      Tag inside the af-llamacpp ECR repository. standup.sh copies the upstream image in
      before this stack is deployed.
  # ⚠️ SEED PARAMETERS (ADR 0072 decision 7). These five are read ONCE, by a Control Plane whose
  # llm catalogue is empty, to create the row this deployment was already serving. After that
  # the catalogue is the declaration and these are ignored — see PARAMETERS-60-engines.md,
  # "The seed parameters".
  LlmModelS3Key:
    Type: String
    Default: ""
    Description: >-
      SEED: key in this stack's models bucket for the model this deployment already runs.
      Empty with LlmEnabled unset means no llm role at all.
  LlmModelFile:
    Type: String
    Default: model.gguf
    Description: >-
      Unused since ADR 0072 — the box mirrors the bucket, so the local path is the S3 key.
      Kept for one release so an existing params file still applies.
  LlmModelIds:
    Type: CommaDelimitedList
    Default: "qwen3-coder-30b-a3b"
    Description: "SEED: the catalogue id for that model. The FIRST is the one that is seeded."
  LlmContextTokens:
    Type: Number
    Default: 32768
    MinValue: 512
    Description: "SEED: that model's context window (llama-server's -c, and what is advertised)."
  LlmMaxOutputTokens:
    Type: Number
    Default: 4096
    MinValue: 256
    Description: >-
      SEED: the output cap advertised with it. It is SUBTRACTED from the context to get the
      usable window, and 0 does not mean unset — see PARAMETERS-60-engines.md.
  LlmExtraArgs:
    Type: CommaDelimitedList
    Default: "-ngl,99,--jinja"
    Description: >-
      Extra llama-server flags, measured on an L4: all layers on the GPU, chat template applied
      (the latter is what makes tool calls work). ⚠️ NEVER -c here: the router passes its own
      command line to every model instance and a command line WINS over the preset, so one -c
      silently gives every model that window (measured). Windows are the catalogue's.
  LlmModelsMax:
    Type: Number
    Default: 1
    MinValue: 0
    Description: >-
      Models the router may hold at once (llama-server --models-max; 0 = unlimited). 1 because
      the router does not know about VRAM: a second 30B Q4 on an L4 does not run slowly, it
      crashes. Raise it only for a box whose models fit side by side.
  LlmApiKeySsmParam:
    Type: String
    Default: /af-ws/engine-llm-key
    Description: >-
      SSM SecureString holding llama-server's --api-key, which the CP presents upstream: the
      second lock on an API that has none of its own. standup.sh creates it. Empty = no key.
  LlmTaskCpu:
    Type: String
    Default: "4096"
    Description: Task vCPU units. 4096 fills a g6.xlarge; keep it under the instance.
  LlmTaskMemory:
    Type: String
    Default: "14336"
    Description: >-
      Task memory (MiB), below the instance's 16 GiB by enough for the ECS agent — a task that
      asks for all of it never places, which reads as a capacity problem.
  LlmAllowedInstanceTypes:
    Type: CommaDelimitedList
    Default: "g6.xlarge"
    Description: >-
      Instance types this role may buy. Declared per role: the floor is a VRAM number (the 30B
      measured 20,943 MiB) and VRAM is not derivable from vCPU or memory.
  LlmGpuCount:
    Type: Number
    Default: 1
    Description: GPUs per box, and the task's GPU resource requirement. 0 = a CPU box (test only).
  LlmVCpuMin:
    Type: Number
    Default: 4
    Description: Lower bound of the instance requirement (vCPU).
  LlmVCpuMax:
    Type: Number
    Default: 8
    Description: Upper bound of the instance requirement (vCPU).
  LlmMemMinMiB:
    Type: Number
    Default: 15000
    Description: Lower bound of the instance requirement (MiB).
  LlmMemMaxMiB:
    Type: Number
    Default: 65536
    Description: Upper bound of the instance requirement (MiB).
  LlmStorageGiB:
    Type: Number
    Default: 120
    Description: >-
      EBS data volume per box when UseLocalStorage is off. MI exposes only the SIZE —
      throughput and IOPS cannot be asked for.
  LlmUseLocalStorage:
    Type: String
    Default: "false"
    AllowedValues: [ "true", "false" ]
    Description: >-
      Use the 250 GB instance store instead of an EBS data volume (ADR 0071 open question 1).
      Off until measured: it is also wiped per box, i.e. a fresh S3 fetch on every cold start.
  LlmScaleInAfter:
    Type: Number
    Default: -2
    Description: >-
      infrastructureOptimization.scaleInAfter in seconds: -2 = leave it at AWS's default,
      -1 = never tidy up, 0-3600 = that many seconds.
  LlmIdleSec:
    Type: Number
    Default: 1800
    Description: How long the engine goes unwanted before the controller stops it.
  LlmStartDeadlineSec:
    Type: Number
    Default: 900
    Description: >-
      How long a start may take before the controller calls it failed. MUST exceed the real
      cold start (measured 527-586 s) or every start is a failure and the cooldown doubles away.
  LlmMode:
    Type: String
    Default: ondemand
    AllowedValues: [ off, on, ondemand ]
    Description: >-
      The engine's initial mode, written into the SSM table as the DEFAULT only — once an
      admin sets it, the stored setting wins (ADR 0070 decision 7).

  # --- the image role (stable-diffusion.cpp — ADR 0071 P1) ----------------------
  # A mirror of the llm block, and a mirror on purpose: instance requirements are declared per
  # role because the floor is a VRAM number (SDXL fp16 measured 7,379 MiB against the 30B's
  # 20,943), and the two roles must never share a box — CUDA does not slow down when VRAM runs
  # out, it crashes.
  ImageEnabled:
    Type: String
    Default: ""
    AllowedValues: [ "", "true", "false" ]
    Description: >-
      Create the image role at all. "" = whatever ImageModelS3Key said (as LlmEnabled).
  ImageDiscoveryName:
    Type: String
    Default: image
    Description: DNS label the image engine answers on, i.e. <ImageDiscoveryName>.<namespace>.
  ImageImageTag:
    Type: String
    Default: master-cuda
    Description: >-
      Tag in the af-sdcpp ECR repository (the doubled word is the image ROLE's container image
      tag). Upstream is amd64 only, which costs nothing: G-family has no arm64 member.
  # SEED parameters, as on the llm role above.
  ImageModelS3Key:
    Type: String
    Default: ""
    Description: >-
      SEED: key in this stack's models bucket for the checkpoint this deployment already runs.
      Empty with ImageEnabled unset means no image role, and standup.sh copies no sdcpp image.
  ImageModelFile:
    Type: String
    Default: sd_xl_base_1.0.safetensors
    Description: "Unused since ADR 0072 (the box mirrors the bucket). Kept for one release."
  ImageModelIds:
    Type: CommaDelimitedList
    Default: "sdxl-base-1.0"
    Description: "SEED: the catalogue id for that checkpoint. The FIRST is the one seeded."
  ImageExtraArgs:
    Type: CommaDelimitedList
    Default: "--diffusion-fa"
    Description: >-
      Extra sd-server flags. --diffusion-fa is what the GPU measurement ran with (1024px in
      20.8-21.0 s, 7.4 GB of VRAM on an L4). There is no --api-key: sd-server has no auth.
  ImageTaskCpu:
    Type: String
    Default: "4096"
    Description: Task vCPU units. 4096 fills a g6.xlarge; keep it under the instance.
  ImageTaskMemory:
    Type: String
    Default: "14336"
    Description: Task memory (MiB), below the instance's 16 GiB by enough for the ECS agent.
  ImageAllowedInstanceTypes:
    Type: CommaDelimitedList
    Default: "g6.xlarge"
    Description: >-
      Instance types this role may buy. SDXL fp16 measured 7,379 MiB of VRAM; whether
      --offload-to-cpu or quantisation fits a smaller card is unmeasured (P4).
  ImageGpuCount:
    Type: Number
    Default: 1
    Description: GPUs per box, and the task's GPU resource requirement. 0 = a CPU box (test only).
  ImageVCpuMin:
    Type: Number
    Default: 4
    Description: Lower bound of the instance requirement (vCPU).
  ImageVCpuMax:
    Type: Number
    Default: 8
    Description: Upper bound of the instance requirement (vCPU).
  ImageMemMinMiB:
    Type: Number
    Default: 15000
    Description: Lower bound of the instance requirement (MiB).
  ImageMemMaxMiB:
    Type: Number
    Default: 65536
    Description: Upper bound of the instance requirement (MiB).
  ImageStorageGiB:
    Type: Number
    Default: 60
    Description: >-
      EBS data volume per box when ImageUseLocalStorage is off. Smaller than the llm role's,
      but not much: the anonymous model volume is re-allocated per task and never reclaimed.
  ImageUseLocalStorage:
    Type: String
    Default: "false"
    AllowedValues: [ "true", "false" ]
    Description: >-
      Use the instance store instead of an EBS data volume. Less pressing than on the llm
      role: 6.5 GB from S3 is under a minute either way.
  ImageScaleInAfter:
    Type: Number
    Default: -2
    Description: >-
      infrastructureOptimization.scaleInAfter in seconds. -1 ("never tidy up") is a trap with
      an anonymous model volume — the kept box re-fetches anyway. See the task definition.
  ImageIdleSec:
    Type: Number
    Default: 900
    Description: >-
      How long the engine goes unwanted before the controller stops it. Half the llm role's
      window: an image is a 21-second request with no conversation around it.
  ImageStartDeadlineSec:
    Type: Number
    Default: 900
    Description: >-
      How long a start may take before the controller calls it failed. sd-server measured 195 s
      from task creation to listen; the variable term is how long a GPU box takes to appear.
  ImageMode:
    Type: String
    Default: ondemand
    AllowedValues: [ off, on, ondemand ]
    Description: The engine's initial mode, written into the SSM table as the DEFAULT only.

  # --- ingest -----------------------------------------------------------------
  HfTokenSecretArn:
    Type: String
    Default: ""
    Description: >-
      Secrets Manager ARN holding HF_TOKEN, read by the INGEST task only (the token is the
      operator's and never lands on an engine box). Empty = ungated repositories only.
  IngestCpu:
    Type: String
    Default: "2048"
    Description: Fargate vCPU units for the ingest task.
  IngestMemory:
    Type: String
    Default: "4096"
    Description: Fargate memory (MiB) for the ingest task.
  IngestDiskGiB:
    Type: Number
    Default: 80
    Description: >-
      Ephemeral storage for the ingest task, i.e. the largest model this deployment can take
      in — it stages the whole file before uploading. 80 GiB covers the 22 GB FLUX checkpoint.
  LogRetentionDays:
    Type: Number
    Default: 14
    Description: CloudWatch retention for the engine and ingest log group.

Mappings:
  # ONE fetch sidecar for both roles (ADR 0072 decision 1(a)): read the active set the Control
  # Plane published to SSM, sync the files the engine is about to load, and write
  # `/models/cmdline` — the model-specific half of the engine's argument list. With PRESET_FILE
  # set it writes a llama.cpp ROUTER preset as well, and then syncs every enabled model rather
  # than only the starting one (ADR 0072 decision 3, phase P1).
  #
  # ⚠️ ParameterNotFound is EMPTY not an error; what each role syncs; and why this is a LITERAL
  # block (`|-`) rather than a folded one, which silently split the shell on the live
  # deployment: PARAMETERS-60-engines.md, "The fetch sidecar". `deploy/local/engine-sidecar-
  # test.sh` runs this script for real against a stub `aws`, and CI runs that.
  Engine:
    fetch:
      script: |-
        set -e
        mkdir -p "$MODELS_DIR"
        V=$(aws ssm get-parameter --name "$ACTIVE_PARAM" --query Parameter.Value --output text 2>/dev/null || true)
        if [ -z "$V" ] || [ "$V" = "None" ]; then
          echo "engine fetch: no active set at $ACTIVE_PARAM - this engine has nothing to load"
          : > "$MODELS_DIR/cmdline"
          exit 0
        fi
        START=$(printf '%s' "$V" | jq -r '.start // ""')
        echo "engine fetch: active set for $ACTIVE_PARAM starts with '$START'"
        if [ -n "$PRESET_FILE" ]; then
          START=$(printf '%s' "$V" | jq -r --arg s "$START" '[(.models[]?|select(.id==$s)|.id),(.models[0].id?)][0]//""')
          printf '%s' "$V" | jq -r '[(.models[]?|.f[]?|if type=="string" then . else .k end),(.loras[]?)]|.[]' > /tmp/keys
        else
          printf '%s' "$V" | jq -r --arg s "$START" '[(.models[]?|select(.id==$s)|.f[]?|if type=="string" then . else .k end),(.loras[]?)]|.[]' > /tmp/keys
        fi
        while read -r k; do
          [ -n "$k" ] || continue
          dst="$MODELS_DIR/$k"
          if [ -s "$dst" ]; then echo "engine fetch: $k is already on this box"; continue; fi
          mkdir -p "$(dirname "$dst")"
          t0=$(date +%s)
          aws s3 cp "s3://$BUCKET/$k" "$dst.part" --only-show-errors
          mv "$dst.part" "$dst"
          echo "engine fetch: $k $(stat -c %s "$dst") bytes in $(( $(date +%s) - t0 ))s"
        done < /tmp/keys
        if [ -n "$PRESET_FILE" ]; then
          mkdir -p "$(dirname "$PRESET_FILE")"
          printf '%s' "$V" | jq -r --arg s "$START" --arg d "$MODELS_DIR" '.models[]? as $m | ($m.a // []) as $a | (["["+$m.id+"]"] + [$m.f[]?|if type=="string" then "model = "+$d+"/"+. else (.g|ltrimstr("-")|ltrimstr("-"))+" = "+$d+"/"+.k end] + (if ($m.c//0)>0 then ["c = "+($m.c|tostring)] else [] end) + [$a|to_entries[]|select(.value|startswith("-"))|(.value|ltrimstr("-")|ltrimstr("-")) as $k|($a[.key+1]//"") as $v|if ($v!="" and ($v|startswith("-")|not)) then $k+" = "+$v else $k+" = true" end] + (if $m.id==$s then ["load-on-startup = true"] else [] end) + [""])[]' > "$PRESET_FILE.part"
          mv "$PRESET_FILE.part" "$PRESET_FILE"
          echo "engine fetch: preset $PRESET_FILE holds $(grep -c '^\[' "$PRESET_FILE" || true) model(s), '$START' loaded at startup"
          if [ -s "$PRESET_FILE" ]; then printf -- '--models-preset %s' "$PRESET_FILE" > "$MODELS_DIR/cmdline.part"; else : > "$MODELS_DIR/cmdline.part"; fi
        else
          printf '%s' "$V" | jq -r --arg s "$START" --arg d "$MODELS_DIR" --arg al "$ALIAS_FLAG" --arg cf "$CTX_FLAG" '(.models[]?|select(.id==$s)) as $m | if $m == null then "" else ([$m.f[]? | if type=="string" then ["-m",($d+"/"+.)] else [.g,($d+"/"+.k)] end] | add // []) + (if $al != "" then [$al,$m.id] else [] end) + (if $cf != "" and (($m.c // 0) > 0) then [$cf,($m.c|tostring)] else [] end) + ($m.a // []) | join(" ") end' > "$MODELS_DIR/cmdline.part"
        fi
        mv "$MODELS_DIR/cmdline.part" "$MODELS_DIR/cmdline"
        echo "engine fetch: cmdline = $(cat "$MODELS_DIR/cmdline")"

Conditions:
  # Whether the ROLE exists — its service, its DNS name, its row in the engine table. WHAT it
  # loads is the catalogue's business (ADR 0072 decision 1), so this no longer keys off a model
  # key; but leaving `*Enabled` at "" keeps the old meaning for one release, so an existing
  # params/60-engines that switched a role off by emptying its model key still does. Why the
  # two-pass stand-up is gone: PARAMETERS-60-engines.md, "The idle wrapper".
  HasLlmModel: !Or
    - !Equals [ !Ref LlmEnabled, "true" ]
    - !And [ !Equals [ !Ref LlmEnabled, "" ], !Not [ !Equals [ !Ref LlmModelS3Key, "" ] ] ]
  HasGpu: !Not [ !Equals [ !Ref LlmGpuCount, 0 ] ]
  UseLocalStorage: !Equals [ !Ref LlmUseLocalStorage, "true" ]
  HasScaleInAfter: !Not [ !Equals [ !Ref LlmScaleInAfter, -2 ] ]
  HasLlmApiKey: !Not [ !Equals [ !Ref LlmApiKeySsmParam, "" ] ]
  HasHfToken: !Not [ !Equals [ !Ref HfTokenSecretArn, "" ] ]
  # The same four for the image role, and the pair that decides the shape of the engine table
  # (0, 1 or 2 rows — the comma between them only exists when both are there).
  HasImageModel: !Or
    - !Equals [ !Ref ImageEnabled, "true" ]
    - !And [ !Equals [ !Ref ImageEnabled, "" ], !Not [ !Equals [ !Ref ImageModelS3Key, "" ] ] ]
  HasImageGpu: !Not [ !Equals [ !Ref ImageGpuCount, 0 ] ]
  ImageUsesLocalStorage: !Equals [ !Ref ImageUseLocalStorage, "true" ]
  HasImageScaleInAfter: !Not [ !Equals [ !Ref ImageScaleInAfter, -2 ] ]
  HasBothModels: !And [ !Condition HasLlmModel, !Condition HasImageModel ]

Resources:
  # --- IAM: three roles, all of them closed inside this stack (ADR 0071 decision 8) ------
  # The CP needs NOTHING new: UpdateService, DescribeServices, the container-instance reads
  # and ssm:GetParameter under /af-ws/* are all already unconditional on CpTaskRole.

  # The role ECS itself assumes to run the Managed Instances fleet.
  InfraRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "af-${AWS::StackName}-mi-infra"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal: { Service: ecs.amazonaws.com }
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AmazonECSInfrastructureRolePolicyForManagedInstances
      # The managed policy's iam:PassRole only covers roles named ecsInstanceRole*, and the
      # instance role below is fleet-named. Without this grant the capacity provider is
      # created and then never launches anything: UnauthorizedOperation on iam:PassRole,
      # visible only in CloudTrail (measured).
      Policies:
        - PolicyName: pass-instance-role
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: iam:PassRole
                Resource: !Sub "arn:aws:iam::${AWS::AccountId}:role/af-${AWS::StackName}-mi-instance"
                Condition: { StringLike: { "iam:PassedToService": "ec2.*" } }

  InstanceRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "af-${AWS::StackName}-mi-instance"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal: { Service: ec2.amazonaws.com }
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AmazonECSInstanceRolePolicyForManagedInstances

  InstanceProfile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      InstanceProfileName: !Sub "af-${AWS::StackName}-mi-instance"
      Roles: [ !Ref InstanceRole ]

  # Engine tasks READ the model catalogue. Ingest WRITES it and reads the HF token. Two roles
  # on purpose: the credential that can overwrite a model file must not sit on a box running a
  # network service, and the operator's HF token must not either.
  #
  # ⚠️ No ssmmessages:* here. The measurement harness has it (the only way to reach a
  # private-subnet engine from a laptop), and a production engine is not something anyone
  # should be able to open a shell into.
  EngineTaskRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "af-${AWS::StackName}-engine-task"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal: { Service: ecs-tasks.amazonaws.com }
            Action: sts:AssumeRole
      Policies:
        - PolicyName: models-read
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: [ s3:GetObject ]
                Resource: !Sub "${ModelsBucket.Arn}/*"
              - Effect: Allow
                Action: [ s3:ListBucket ]
                Resource: !GetAtt ModelsBucket.Arn
              # The active set the Control Plane publishes (ADR 0072 decision 2). Scoped to the
              # two parameters this stack's own engines read, and granted INSIDE this stack —
              # ADR 0071 decision 8's rule that a new permission never leaves the stack that
              # needs it. The CP side needs nothing new: its ssm:PutParameter is already
              # /af-ws/* (20-platform, Sid SsmWorkspaceParams).
              - Effect: Allow
                Action: [ ssm:GetParameter ]
                Resource: !Sub "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter${EnginesSsmParamName}/*"

  IngestTaskRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "af-${AWS::StackName}-ingest-task"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal: { Service: ecs-tasks.amazonaws.com }
            Action: sts:AssumeRole
      Policies:
        - PolicyName: models-write
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: [ s3:PutObject, s3:AbortMultipartUpload, s3:GetObject ]
                Resource: !Sub "${ModelsBucket.Arn}/*"
              - Effect: Allow
                Action: [ s3:ListBucket ]
                Resource: !GetAtt ModelsBucket.Arn

  # --- the model catalogue ------------------------------------------------------
  # S3 at $0.025/GB-month, read for free through 00-network's gateway endpoint (the fetch
  # never touches the NAT). Versioning is off deliberately: a 17 GB model kept in duplicate for
  # every re-ingest is a bill nobody meant to sign, and the sha256 identifies a file (0071
  # decision 10). The layout is ComfyUI's, so all three engines read one tree (0072 decision 2).
  ModelsBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      BucketName: !Sub "af-${AWS::StackName}-models-${AWS::AccountId}"
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault: { SSEAlgorithm: AES256 }
      LifecycleConfiguration:
        Rules:
          - Id: abort-incomplete-uploads
            Status: Enabled
            AbortIncompleteMultipartUpload: { DaysAfterInitiation: 3 }

  LogGroup:
    Type: AWS::Logs::LogGroup
    DeletionPolicy: Delete
    Properties:
      LogGroupName: !Sub "/af/${AWS::StackName}/engines"
      RetentionInDays: !Ref LogRetentionDays

  # Reachability IS the access control for both engines: port 8080 from the CP security group
  # and nothing else — no load balancer, private subnets, no public IP (ADR 0070 decision 14,
  # ADR 0071 decision 4). llama-server's --api-key is a second lock; sd-server has no
  # authentication option at all, so for the image role this group is the whole of it.
  EngineSg:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: !Sub "af-${AWS::StackName} inference engines (CP to engine, 8080)"
      VpcId: !ImportValue { "Fn::Sub": "${NetworkStackName}-VpcId" }
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 8080
          ToPort: 8080
          SourceSecurityGroupId: !ImportValue { "Fn::Sub": "${NetworkStackName}-CpSgId" }
          Description: engine port from the Control Plane only
      Tags: [ { Key: Name, Value: !Sub "af-${AWS::StackName}-engine-sg" } ]

  # --- capacity: one provider per engine role (ADR 0071 decision 2) -------------
  # ⚠️ Why the two roles never share a box, and why two providers want 16 of the G-family
  # quota: PARAMETERS-60-engines.md, "The capacity providers" and "The G-family quota".
  LlmCapacityProvider:
    Type: AWS::ECS::CapacityProvider
    Properties:
      Name: !Sub "af-${AWS::StackName}-llm"
      ClusterName: !ImportValue { "Fn::Sub": "${PlatformStackName}-ClusterName" }
      ManagedInstancesProvider:
        InfrastructureRoleArn: !GetAtt InfraRole.Arn
        PropagateTags: CAPACITY_PROVIDER
        InfrastructureOptimization: !If
          - HasScaleInAfter
          - ScaleInAfter: !Ref LlmScaleInAfter
          - !Ref "AWS::NoValue"
        InstanceLaunchTemplate:
          Ec2InstanceProfileArn: !GetAtt InstanceProfile.Arn
          CapacityOptionType: ON_DEMAND
          NetworkConfiguration:
            Subnets: !Split [ ",", !ImportValue { "Fn::Sub": "${NetworkStackName}-PrivateSubnets" } ]
            SecurityGroups: [ !Ref EngineSg ]
          StorageConfiguration: !If
            - UseLocalStorage
            - !Ref "AWS::NoValue"
            - StorageSizeGiB: !Ref LlmStorageGiB
          LocalStorageConfiguration: !If
            - UseLocalStorage
            - UseLocalStorage: true
            - !Ref "AWS::NoValue"
          InstanceRequirements:
            VCpuCount: { Min: !Ref LlmVCpuMin, Max: !Ref LlmVCpuMax }
            MemoryMiB: { Min: !Ref LlmMemMinMiB, Max: !Ref LlmMemMaxMiB }
            BurstablePerformance: excluded
            AllowedInstanceTypes: !Ref LlmAllowedInstanceTypes
            # AcceleratorCount is refused without AcceleratorTypes (measured).
            AcceleratorCount: !If [ HasGpu, { Min: !Ref LlmGpuCount, Max: !Ref LlmGpuCount }, !Ref "AWS::NoValue" ]
            AcceleratorTypes: !If [ HasGpu, [ gpu ], !Ref "AWS::NoValue" ]
            AcceleratorManufacturers: !If [ HasGpu, [ nvidia ], !Ref "AWS::NoValue" ]
      Tags:
        # Cost allocation: engine hours are a COMPONENT cost, shown as such and never
        # apportioned to a member (ADR 0048, ADR 0071 decision 9).
        - { Key: af-role, Value: engine-llm }
        - { Key: af-pool, Value: !ImportValue { "Fn::Sub": "${PlatformStackName}-ClusterName" } }

  # The image role's own capacity. Created even when no image model is staged: a capacity
  # provider costs nothing while nothing runs on it, it is named in the association list
  # below (which is replaced wholesale, so it cannot be added conditionally without churning
  # the list), and deleting one that a service used is a stack update that has to wait for
  # the service to go first.
  ImageCapacityProvider:
    Type: AWS::ECS::CapacityProvider
    Properties:
      Name: !Sub "af-${AWS::StackName}-image"
      ClusterName: !ImportValue { "Fn::Sub": "${PlatformStackName}-ClusterName" }
      ManagedInstancesProvider:
        InfrastructureRoleArn: !GetAtt InfraRole.Arn
        PropagateTags: CAPACITY_PROVIDER
        InfrastructureOptimization: !If
          - HasImageScaleInAfter
          - ScaleInAfter: !Ref ImageScaleInAfter
          - !Ref "AWS::NoValue"
        InstanceLaunchTemplate:
          Ec2InstanceProfileArn: !GetAtt InstanceProfile.Arn
          CapacityOptionType: ON_DEMAND
          NetworkConfiguration:
            Subnets: !Split [ ",", !ImportValue { "Fn::Sub": "${NetworkStackName}-PrivateSubnets" } ]
            SecurityGroups: [ !Ref EngineSg ]
          StorageConfiguration: !If
            - ImageUsesLocalStorage
            - !Ref "AWS::NoValue"
            - StorageSizeGiB: !Ref ImageStorageGiB
          LocalStorageConfiguration: !If
            - ImageUsesLocalStorage
            - UseLocalStorage: true
            - !Ref "AWS::NoValue"
          InstanceRequirements:
            VCpuCount: { Min: !Ref ImageVCpuMin, Max: !Ref ImageVCpuMax }
            MemoryMiB: { Min: !Ref ImageMemMinMiB, Max: !Ref ImageMemMaxMiB }
            BurstablePerformance: excluded
            AllowedInstanceTypes: !Ref ImageAllowedInstanceTypes
            AcceleratorCount: !If [ HasImageGpu, { Min: !Ref ImageGpuCount, Max: !Ref ImageGpuCount }, !Ref "AWS::NoValue" ]
            AcceleratorTypes: !If [ HasImageGpu, [ gpu ], !Ref "AWS::NoValue" ]
            AcceleratorManufacturers: !If [ HasImageGpu, [ nvidia ], !Ref "AWS::NoValue" ]
      Tags:
        - { Key: af-role, Value: engine-image }
        - { Key: af-pool, Value: !ImportValue { "Fn::Sub": "${PlatformStackName}-ClusterName" } }

  # ⚠️ This REPLACES the cluster's provider list, and its empty default strategy is
  # load-bearing: PARAMETERS-60-engines.md, "The capacity providers".
  Associations:
    Type: AWS::ECS::ClusterCapacityProviderAssociations
    Properties:
      Cluster: !ImportValue { "Fn::Sub": "${PlatformStackName}-ClusterName" }
      CapacityProviders: [ FARGATE, FARGATE_SPOT, !Ref LlmCapacityProvider, !Ref ImageCapacityProvider ]
      DefaultCapacityProviderStrategy: []

  # --- ingest: HF -> S3, once, out of the start path (ADR 0071 decision 3) ------
  # A Fargate task an operator runs by hand. The `run-task` recipe, the override trap and why
  # the sha256 is not optional: PARAMETERS-60-engines.md, "Running the ingest task by hand".
  IngestTaskDef:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub "af-${AWS::StackName}-ingest"
      RequiresCompatibilities: [ FARGATE ]
      NetworkMode: awsvpc
      Cpu: !Ref IngestCpu
      Memory: !Ref IngestMemory
      EphemeralStorage: { SizeInGiB: !Ref IngestDiskGiB }
      ExecutionRoleArn: !ImportValue { "Fn::Sub": "${PlatformStackName}-ExecRoleArn" }
      TaskRoleArn: !GetAtt IngestTaskRole.Arn
      Volumes: [ { Name: scratch } ]
      ContainerDefinitions:
        - Name: fetch
          Essential: false
          Image: curlimages/curl:latest
          # The shared volume is root-owned and curlimages/curl runs as uid 100, which
          # surfaces as the useless "curl: (23) client returned ERROR on write" (measured).
          User: "0"
          EntryPoint: [ "sh", "-c" ]
          Command:
            - >-
              set -e;
              [ -n "$URL" ] || { echo "ingest: URL is required"; exit 2; };
              start=$(date +%s);
              if [ -n "$HF_TOKEN" ]; then set -- -H "Authorization: Bearer $HF_TOKEN"; else set --; fi;
              curl -fsSL "$@" -o /scratch/blob "$URL";
              size=$(stat -c %s /scratch/blob);
              secs=$(( $(date +%s) - start ));
              echo "ingest: fetched $size bytes in ${secs}s";
              if [ -n "$SHA256" ]; then
                got=$(sha256sum /scratch/blob | cut -d" " -f1);
                [ "$got" = "$SHA256" ] || { echo "ingest: sha256 mismatch: got $got want $SHA256"; exit 1; };
                echo "ingest: sha256 ok $got";
              else
                echo "ingest: WARNING no SHA256 given — the upload is unverified";
              fi
          Environment:
            - { Name: URL, Value: "" }
            - { Name: SHA256, Value: "" }
          Secrets: !If
            - HasHfToken
            - - { Name: HF_TOKEN, ValueFrom: !Ref HfTokenSecretArn }
            - !Ref "AWS::NoValue"
          MountPoints: [ { SourceVolume: scratch, ContainerPath: /scratch } ]
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref LogGroup
              awslogs-region: !Ref "AWS::Region"
              awslogs-stream-prefix: ingest-fetch
        - Name: upload
          Essential: true
          Image: public.ecr.aws/aws-cli/aws-cli:latest
          EntryPoint: [ "sh", "-c" ]
          Command:
            - !Sub >-
              set -e;
              [ -n "$KEY" ] || { echo "ingest: KEY is required"; exit 2; };
              start=$(date +%s);
              aws s3 cp /scratch/blob "s3://${ModelsBucket}/$KEY" --only-show-errors;
              echo "ingest: uploaded $KEY in $(( $(date +%s) - start ))s";
          Environment: [ { Name: KEY, Value: "" } ]
          MountPoints: [ { SourceVolume: scratch, ContainerPath: /scratch } ]
          # SUCCESS, not COMPLETE: a fetch that failed its checksum must not be uploaded.
          DependsOn: [ { ContainerName: fetch, Condition: SUCCESS } ]
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref LogGroup
              awslogs-region: !Ref "AWS::Region"
              awslogs-stream-prefix: ingest-upload

  # --- the llm engine -----------------------------------------------------------
  LlmTaskDef:
    Type: AWS::ECS::TaskDefinition
    Condition: HasLlmModel
    Properties:
      Family: !Sub "af-${AWS::StackName}-llm"
      RequiresCompatibilities: [ MANAGED_INSTANCES ]
      NetworkMode: awsvpc
      Cpu: !Ref LlmTaskCpu
      Memory: !Ref LlmTaskMemory
      ExecutionRoleArn: !ImportValue { "Fn::Sub": "${PlatformStackName}-ExecRoleArn" }
      TaskRoleArn: !GetAtt EngineTaskRole.Arn
      Volumes:
        # An ANONYMOUS host volume — an empty `Host`, no SourcePath. Both halves are measured
        # and both are counter-intuitive; the measurements, and why decision 7(c)'s "warm box"
        # stays unproven, are in PARAMETERS-60-engines.md, "The model volume".
        - Name: models
          Host: {}
      ContainerDefinitions:
        # The fetch runs to completion before llama-server starts, and it logs how long it
        # took — that line is the S3 half of the cold start, measured at 104-147 MB/s and
        # running concurrently with the image pull. The script is shared with the image role
        # (see Mappings); everything role-specific is in the environment below.
        - Name: fetch
          Essential: false
          Image: public.ecr.aws/aws-cli/aws-cli:latest
          EntryPoint: [ "sh", "-c" ]
          Command: [ !FindInMap [ Engine, fetch, script ] ]
          Environment:
            - { Name: MODELS_DIR, Value: /models }
            - { Name: BUCKET, Value: !Ref ModelsBucket }
            - { Name: ACTIVE_PARAM, Value: !Sub "${EnginesSsmParamName}/llm/active" }
            # ROUTER MODE (ADR 0072 decision 3): the model, its window and its own flags go into
            # a preset section named after the catalogue id, so `-m` / `--alias` / `-c` leave the
            # command line entirely — which is why both flags below are empty here now. The
            # router names each instance with --alias itself (measured).
            - { Name: PRESET_FILE, Value: /models/llm/presets.ini }
            - { Name: ALIAS_FLAG, Value: "" }
            - { Name: CTX_FLAG, Value: "" }
          MountPoints: [ { SourceVolume: models, ContainerPath: /models } ]
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref LogGroup
              awslogs-region: !Ref "AWS::Region"
              awslogs-stream-prefix: llm-fetch
        - Name: llama
          Essential: true
          # ⚠️ The image must ALREADY be in that repository when this stack is created, or the
          # stack blocks in CREATE_IN_PROGRESS: PARAMETERS-60-engines.md, "The engine services".
          Image: !Sub
            - "${Uri}:${Tag}"
            - Uri: !ImportValue { "Fn::Sub": "${PlatformStackName}-EcrLlamacppUri" }
              Tag: !Ref LlmImageTag
          PortMappings: [ { ContainerPort: 8080 } ]
          # ⚠️ The idle wrapper (ADR 0072 decision 1(a)): an engine with nothing to load has to
          # come up rather than fail, or CloudFormation waits for a service that never
          # stabilises. Why the binary path is spelled out, and why $(cat …) is unquoted:
          # PARAMETERS-60-engines.md, "The idle wrapper".
          EntryPoint: [ "sh", "-c" ]
          Command:
            - !Sub
              - >-
                if [ -s /models/cmdline ]; then exec /app/llama-server --host 0.0.0.0 --port 8080
                --models-max ${LlmModelsMax} ${Extra} $(cat /models/cmdline);
                else echo "llm: no model in the catalogue — idling"; sleep infinity; fi
              - Extra: !Join [ " ", !Ref LlmExtraArgs ]
          ResourceRequirements: !If [ HasGpu, [ { Type: GPU, Value: !Ref LlmGpuCount } ], !Ref "AWS::NoValue" ]
          MountPoints: [ { SourceVolume: models, ContainerPath: /models } ]
          DependsOn: [ { ContainerName: fetch, Condition: SUCCESS } ]
          Environment:
            # -hf is never used (decision 3), but llama-server writes here regardless of
            # how the model arrived; pointing it at /tmp keeps it off the model volume.
            - { Name: LLAMA_CACHE, Value: /tmp/llama-cache }
          Secrets: !If
            - HasLlmApiKey
            - - Name: LLAMA_API_KEY
                ValueFrom: !Sub "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter${LlmApiKeySsmParam}"
            - !Ref "AWS::NoValue"
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref LogGroup
              awslogs-region: !Ref "AWS::Region"
              awslogs-stream-prefix: llm

  # A Cloud Map DNS service, not Service Connect: the CP reaches the engine at
  # http://<name>.<namespace>:8080 through the VPC resolver, so the gateway needs no sidecar
  # and no /etc/hosts snapshot (ADR 0070 decision 8). At desired 0 the name has no A record
  # at all — the normal state of an engine nobody is using, not an error.
  LlmDiscovery:
    Type: AWS::ServiceDiscovery::Service
    Condition: HasLlmModel
    Properties:
      Name: !Ref LlmDiscoveryName
      NamespaceId: !ImportValue { "Fn::Sub": "${PlatformStackName}-NamespaceId" }
      DnsConfig:
        DnsRecords: [ { Type: A, TTL: 10 } ]
      # Without it Cloud Map refuses the registration for an ECS service that has no load
      # balancer.
      HealthCheckCustomConfig: { FailureThreshold: 1 }

  LlmService:
    Type: AWS::ECS::Service
    Condition: HasLlmModel
    DependsOn: Associations
    Properties:
      ServiceName: !Sub "af-${AWS::StackName}-llm"
      Cluster: !ImportValue { "Fn::Sub": "${PlatformStackName}-ClusterArn" }
      TaskDefinition: !Ref LlmTaskDef
      # ⚠️ DesiredCount is deliberately ABSENT and this is load-bearing — declaring `0` would
      # kill a GPU box mid-answer on every stack update. No LaunchType either: it is mutually
      # exclusive with a capacity provider strategy. PARAMETERS-60-engines.md, "The engine
      # services".
      CapacityProviderStrategy:
        - { CapacityProvider: !Ref LlmCapacityProvider, Weight: 1 }
      # One engine behind one DNS name: two tasks during a redeploy would mean two A records
      # and requests split between a warm engine and a cold one, on a box that costs
      # $1.26/hour. Stop the old task first.
      DeploymentConfiguration: { MinimumHealthyPercent: 0, MaximumPercent: 100 }
      NetworkConfiguration:
        AwsvpcConfiguration:
          AssignPublicIp: DISABLED
          Subnets: !Split [ ",", !ImportValue { "Fn::Sub": "${NetworkStackName}-PrivateSubnets" } ]
          SecurityGroups: [ !Ref EngineSg ]
      ServiceRegistries:
        - RegistryArn: !GetAtt LlmDiscovery.Arn
      PropagateTags: SERVICE
      Tags:
        - { Key: af-pool, Value: !ImportValue { "Fn::Sub": "${PlatformStackName}-ClusterName" } }
        - { Key: af-role, Value: engine-llm }

  # --- the image engine (ADR 0071 P1) -------------------------------------------
  ImageTaskDef:
    Type: AWS::ECS::TaskDefinition
    Condition: HasImageModel
    Properties:
      Family: !Sub "af-${AWS::StackName}-image"
      RequiresCompatibilities: [ MANAGED_INSTANCES ]
      NetworkMode: awsvpc
      Cpu: !Ref ImageTaskCpu
      Memory: !Ref ImageTaskMemory
      ExecutionRoleArn: !ImportValue { "Fn::Sub": "${PlatformStackName}-ExecRoleArn" }
      TaskRoleArn: !GetAtt EngineTaskRole.Arn
      Volumes:
        # `Host: {}`, for the reasons measured on the llm role above.
        - Name: models
          Host: {}
      ContainerDefinitions:
        - Name: fetch
          Essential: false
          Image: public.ecr.aws/aws-cli/aws-cli:latest
          EntryPoint: [ "sh", "-c" ]
          Command: [ !FindInMap [ Engine, fetch, script ] ]
          Environment:
            - { Name: MODELS_DIR, Value: /models }
            - { Name: BUCKET, Value: !Ref ModelsBucket }
            - { Name: ACTIVE_PARAM, Value: !Sub "${EnginesSsmParamName}/image/active" }
            # sd-server holds ONE checkpoint, has no --alias, no -c and no router: all three
            # empty, so the shared script writes a plain `-m` command line and syncs only the
            # selected checkpoint.
            - { Name: PRESET_FILE, Value: "" }
            - { Name: ALIAS_FLAG, Value: "" }
            - { Name: CTX_FLAG, Value: "" }
          MountPoints: [ { SourceVolume: models, ContainerPath: /models } ]
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref LogGroup
              awslogs-region: !Ref "AWS::Region"
              awslogs-stream-prefix: image-fetch
        - Name: sd
          Essential: true
          Image: !Sub
            - "${Uri}:${Tag}"
            - Uri: !ImportValue { "Fn::Sub": "${PlatformStackName}-EcrSdcppUri" }
              Tag: !Ref ImageImageTag
          # ⚠️ Why the entry point is overridden, why sd-server's flags are spelled differently
          # and why --lora-model-dir is stated: PARAMETERS-60-engines.md, "The idle wrapper".
          EntryPoint: [ "sh", "-c" ]
          Command:
            - !Sub
              - >-
                mkdir -p /models/image/loras;
                if [ -s /models/cmdline ]; then exec /sd-server --listen-ip 0.0.0.0
                --listen-port 8080 --lora-model-dir /models/image/loras
                ${Extra} $(cat /models/cmdline);
                else echo "image: no checkpoint in the catalogue — idling"; sleep infinity; fi
              - Extra: !Join [ " ", !Ref ImageExtraArgs ]
          ResourceRequirements: !If [ HasImageGpu, [ { Type: GPU, Value: !Ref ImageGpuCount } ], !Ref "AWS::NoValue" ]
          MountPoints: [ { SourceVolume: models, ContainerPath: /models } ]
          DependsOn: [ { ContainerName: fetch, Condition: SUCCESS } ]
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref LogGroup
              awslogs-region: !Ref "AWS::Region"
              awslogs-stream-prefix: image

  ImageDiscovery:
    Type: AWS::ServiceDiscovery::Service
    Condition: HasImageModel
    Properties:
      Name: !Ref ImageDiscoveryName
      NamespaceId: !ImportValue { "Fn::Sub": "${PlatformStackName}-NamespaceId" }
      DnsConfig:
        DnsRecords: [ { Type: A, TTL: 10 } ]
      HealthCheckCustomConfig: { FailureThreshold: 1 }

  ImageService:
    Type: AWS::ECS::Service
    Condition: HasImageModel
    DependsOn: Associations
    Properties:
      ServiceName: !Sub "af-${AWS::StackName}-image"
      Cluster: !ImportValue { "Fn::Sub": "${PlatformStackName}-ClusterArn" }
      TaskDefinition: !Ref ImageTaskDef
      # DesiredCount deliberately ABSENT — see the llm service. standup.sh scales a NEWLY
      # created service to 0; a later update leaves whatever count the controller set.
      CapacityProviderStrategy:
        - { CapacityProvider: !Ref ImageCapacityProvider, Weight: 1 }
      DeploymentConfiguration: { MinimumHealthyPercent: 0, MaximumPercent: 100 }
      NetworkConfiguration:
        AwsvpcConfiguration:
          AssignPublicIp: DISABLED
          Subnets: !Split [ ",", !ImportValue { "Fn::Sub": "${NetworkStackName}-PrivateSubnets" } ]
          SecurityGroups: [ !Ref EngineSg ]
      ServiceRegistries:
        - RegistryArn: !GetAtt ImageDiscovery.Arn
      PropagateTags: SERVICE
      Tags:
        - { Key: af-pool, Value: !ImportValue { "Fn::Sub": "${PlatformStackName}-ClusterName" } }
        - { Key: af-role, Value: engine-image }

  # --- the engine table the CP reads at startup (ADR 0071 decision 8) -----------
  # 30-ingress has 9.2 KB of template budget left and six parameters per engine would eat a
  # third of it, so the CP is handed ONE parameter name and reads the table from SSM. The
  # name has to be under /af-ws/ — that is the whole of the CP task role's SSM read scope.
  EnginesParam:
    Type: AWS::SSM::Parameter
    Properties:
      Name: !Ref EnginesSsmParamName
      Type: String
      Description: !Sub "agent-fleet engine table written by ${AWS::StackName} (ADR 0071)"
  # The table holds 0, 1 or 2 rows — each role is staged independently — so it is assembled
  # from two fragments and a separator that exists only when both do. What `api` decides, why
  # the image row's health path is /v1/models rather than /health, and why the llm row carries
  # its context window: PARAMETERS-60-engines.md, "The engine table".
      Value: !Sub
        - '{"engines":[${Llm}${Sep}${Image}]}'
        - Llm: !If
            - HasLlmModel
            - !Sub
              - >-
                {"key":"llm","api":"chat","service":"${Service}","capacityProvider":"${Cp}",
                "url":"http://${LlmDiscoveryName}.${ServiceConnectNamespace}:8080",
                "health":"/health","warmPath":"/models","provider":"llamacpp","models":["${Models}"],
                "contextTokens":${LlmContextTokens},"maxOutputTokens":${LlmMaxOutputTokens},
                "modelS3Key":"${LlmModelS3Key}",
                "apiKeyParam":"${LlmApiKeySsmParam}","idleSec":${LlmIdleSec},
                "startDeadlineSec":${LlmStartDeadlineSec},"mode":"${LlmMode}"}
              - Service: !GetAtt LlmService.Name
                Cp: !Sub "af-${AWS::StackName}-llm"
                Models: !Join [ '","', !Ref LlmModelIds ]
            - ""
          Image: !If
            - HasImageModel
            - !Sub
              - >-
                {"key":"image","api":"images","service":"${Service}","capacityProvider":"${Cp}",
                "url":"http://${ImageDiscoveryName}.${ServiceConnectNamespace}:8080",
                "health":"/v1/models","provider":"sdcpp","models":["${Models}"],
                "modelS3Key":"${ImageModelS3Key}",
                "apiKeyParam":"","idleSec":${ImageIdleSec},
                "startDeadlineSec":${ImageStartDeadlineSec},"mode":"${ImageMode}"}
              - Service: !GetAtt ImageService.Name
                Cp: !Sub "af-${AWS::StackName}-image"
                Models: !Join [ '","', !Ref ImageModelIds ]
            - ""
          Sep: !If [ HasBothModels, ",", "" ]

Outputs:
  # The one value standup.sh feeds to 30-ingress. Non-empty by construction (a parameter
  # with a non-empty default), which is what check-cfn-exports.py insists on.
  EnginesSsmParam:
    Description: AF_ENGINES_SSM_PARAM for the Control Plane (the engine table's SSM name).
    Value: !Ref EnginesSsmParamName
    Export: { Name: !Sub "${AWS::StackName}-EnginesSsmParam" }
  LlmServiceName:
    Description: >-
      The llm engine's ECS service name (what the controller drives), or "-" while no model
      is staged. Never empty: an empty Export fails the whole stack (check-cfn-exports.py).
    Value: !If [ HasLlmModel, !GetAtt LlmService.Name, "-" ]
    Export: { Name: !Sub "${AWS::StackName}-LlmServiceName" }
  LlmCapacityProviderName:
    Description: The llm capacity provider, i.e. what marks a container instance as NOT a workspace slot.
    Value: !Ref LlmCapacityProvider
    Export: { Name: !Sub "${AWS::StackName}-LlmCapacityProviderName" }
  ImageServiceName:
    Description: >-
      The image engine's ECS service name, or "-" while no checkpoint is staged. Never empty
      for the same reason as the llm one: an empty Export fails the whole stack.
    Value: !If [ HasImageModel, !GetAtt ImageService.Name, "-" ]
    Export: { Name: !Sub "${AWS::StackName}-ImageServiceName" }
  ImageCapacityProviderName:
    Description: The image capacity provider (the other thing that marks a box as NOT a workspace slot).
    Value: !Ref ImageCapacityProvider
    Export: { Name: !Sub "${AWS::StackName}-ImageCapacityProviderName" }
  ModelsBucket:
    Description: S3 bucket the models are staged in (ingest writes, engines read).
    Value: !Ref ModelsBucket
    Export: { Name: !Sub "${AWS::StackName}-ModelsBucket" }
  IngestTaskDefFamily:
    Description: Task definition family to `aws ecs run-task` for taking a model in from Hugging Face.
    Value: !Ref IngestTaskDef
    Export: { Name: !Sub "${AWS::StackName}-IngestTaskDefFamily" }
