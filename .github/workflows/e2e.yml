# E2E（docs/dev/10 §10.4): Workspace イメージを build し、L1（e2e-smoke.sh: CLI 実版 =
# Dockerfile ARG ピン等）→ L2（e2e/: CP をヘッドレス起動して workspace/session 疎通）→
# L3（console-e2e/: Playwright でブラウザ→CP→コンテナの縦串）を検証する。イメージ build が
# 重いので ci.yml から分離し、関係パスの変更時 + 週次 cron（コード無変更でも上流 CLI の
# 破壊を検出）+ 手動のみ実行する。L4（実クレデンシャルの claude 疎通）は枠を消費するため
# workflow_dispatch の live 入力を立てた時だけ走る。
#
# エージェント CLI の contract / ドリフト検知はここには置かない（別ファイル・イメージ
# build 不要）: claude-tui-contract.yml / codex-contract.yml / opencode-contract.yml。
# 版ズレそのものの監視は cli-drift.yml。構成の全体像は docs/dev/10-development.md。
# 注: rtk は常時焼き込み（BAKE_RTK=1 既定・ARG ピン）なので、CI ビルドのイメージにも
# rtk が入り、L1 スモークが同梱を検証する（旧ホスト vendor 経路は廃止）。
name: e2e

on:
  push:
    paths:
      - "workspace/**"
      - "control-plane/**"
      - "console/**"
      - "e2e/**"
      - "console-e2e/**"
      - "deploy/local/e2e-smoke.sh"
      - ".github/workflows/e2e.yml"
  pull_request:
    paths:
      - "workspace/**"
      - "control-plane/**"
      - "console/**"
      - "e2e/**"
      - "console-e2e/**"
      - "deploy/local/e2e-smoke.sh"
      - ".github/workflows/e2e.yml"
  schedule:
    - cron: "0 21 * * 0" # 毎週月曜 6:00 JST — 上流 CLI / base image の破壊検出
  workflow_dispatch:
    inputs:
      # 課金/枠消費を伴う入力は**そのワークフローが回すエージェント 1 種類だけ**を指す。
      # 以前ここの live 入力を codex の Tier2 ジョブと共有しており、1 回の dispatch で
      # Anthropic と ChatGPT の枠が同時に減った（片方だけ回せなかった）。codex は
      # codex-contract.yml へ分離済み ＝ この入力は claude の live-smoke 専用。
      live:
        description: "L4: claude 疎通スモーク（live-smoke）を実行する — Anthropic のサブスク枠 / API 課金を消費"
        type: boolean
        default: false

concurrency:
  group: e2e-${{ github.ref }}
  cancel-in-progress: true

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    env:
      WS_IMAGE: agent-fleet/workspace:dev
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: control-plane/go.mod
          cache-dependency-path: control-plane/go.sum
      - uses: docker/setup-buildx-action@v3
      - name: build workspace image
        uses: docker/build-push-action@v6
        with:
          context: workspace
          tags: ${{ env.WS_IMAGE }}
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: image smoke (L1)
        run: deploy/local/e2e-smoke.sh "$WS_IMAGE"
      - name: vet e2e module
        working-directory: e2e
        run: go vet -tags e2e ./...
      - name: fleet e2e (L2)
        working-directory: e2e
        env:
          E2E_REQUIRE: "1"
        run: go test -v -tags e2e -timeout 15m ./...

  ui-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    env:
      WS_IMAGE: agent-fleet/workspace:dev
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: control-plane/go.mod
          cache-dependency-path: control-plane/go.sum
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            console/package-lock.json
            console-e2e/package-lock.json
      - uses: docker/setup-buildx-action@v3
      - name: build workspace image
        uses: docker/build-push-action@v6
        with:
          context: workspace
          tags: ${{ env.WS_IMAGE }}
          load: true
          cache-from: type=gha
          # e2e ジョブが cache-to を担う（同じ内容の二重書き込みを避ける）
      - name: build console
        working-directory: console
        run: |
          npm ci
          NODE_OPTIONS=--max-old-space-size=3072 npm run build
      - name: install playwright
        working-directory: console-e2e
        run: |
          npm ci
          npx playwright install --with-deps chromium
      - name: console ui e2e (L3)
        working-directory: console-e2e
        env:
          E2E_REQUIRE: "1"
        run: npx playwright test
      - name: upload traces & CP log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ui-e2e-results
          path: console-e2e/test-results/
          if-no-files-found: ignore

  live-smoke:
    # L4: 実 API キーで claude -p の疎通を見る。手動 + 明示 opt-in のみ（課金あり）。
    # 注: これは headless の `claude -p` ＝ TUI もフッタも描画されない。状態検出の契約は
    # ここでは見えないので claude-tui-contract.yml が別途見る。
    if: github.event_name == 'workflow_dispatch' && inputs.live
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      WS_IMAGE: agent-fleet/workspace:dev
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: control-plane/go.mod
          cache-dependency-path: control-plane/go.sum
      - uses: docker/setup-buildx-action@v3
      - name: build workspace image
        uses: docker/build-push-action@v6
        with:
          context: workspace
          tags: ${{ env.WS_IMAGE }}
          load: true
          cache-from: type=gha
      - name: guard secret
        env:
          KEY: ${{ secrets.E2E_ANTHROPIC_API_KEY }}
          TOKEN: ${{ secrets.E2E_CLAUDE_OAUTH_TOKEN }}
        run: |
          if [ -z "$KEY" ] && [ -z "$TOKEN" ]; then
            echo "::error::secrets.E2E_ANTHROPIC_API_KEY（API キー）か secrets.E2E_CLAUDE_OAUTH_TOKEN（claude setup-token の OAuth トークン）のどちらかを登録してください"
            exit 1
          fi
      - name: claude live smoke (L4)
        working-directory: e2e
        env:
          E2E_REQUIRE: "1"
          E2E_ANTHROPIC_API_KEY: ${{ secrets.E2E_ANTHROPIC_API_KEY }}
          E2E_CLAUDE_OAUTH_TOKEN: ${{ secrets.E2E_CLAUDE_OAUTH_TOKEN }}
        run: go test -v -tags e2e -timeout 15m -run TestClaudeLive ./...
